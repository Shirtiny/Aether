# 容器更新历史

> 用于记录 Aether 更新前后的容器镜像快照，便于更新后核对和必要时回滚参考。只记录非敏感信息。

## 2026-07-04 21:41:33 UTC - 更新前快照

- 工作目录：`/opt/stacks/aether`
- Compose 文件：`docker-compose.yml`
- 记录方式：`docker ps`、`docker inspect`（只读）
- 操作说明：本次仅记录当前容器版本；未执行 `pull`、`up`、`restart`、`update` 等变更操作。
- 范围：当前 `aether` compose 项目正在运行的容器。`mysql` 服务属于 profile，当前未运行，未记录为运行快照。

| 服务 | 容器名 | 状态 | 镜像标签 | 版本/修订 | 运行镜像 ID / 本地 RepoDigest | Manifest Digest | 容器 ID | 容器创建时间 | 启动时间 |
|---|---|---|---|---|---|---|---|---|---|
| app | `aether-app` | `running / healthy` | `ghcr.io/shirtiny/aether:latest` | `version=latest`<br>`revision=fc4e73f0ed275cf6be70f85dcd92b5dbb30df09f`<br>`image_created=2026-06-27T11:06:05.775Z` | `sha256:f1970edb3276a120d2ecef85300f277eb949ac1285fc270b04829dc41a7d2315`<br>`ghcr.io/shirtiny/aether@sha256:f1970edb3276a120d2ecef85300f277eb949ac1285fc270b04829dc41a7d2315` | `sha256:ba984d1f4ba6bef94f5b89f4ee3d9bea1e8afbe75e23e68aa4375e8b4c56aa0c` | `1c804e90a03a69cd4ac4af78f00174281443b6f9eecc3b715a86eb14054f43c9` | `2026-07-03T03:13:22.518457388Z` | `2026-07-03T03:13:22.64022712Z` |
| postgres | `aether-postgres` | `running / healthy` | `postgres:15` | `PG_VERSION=15.18-1.pgdg13+1` | `sha256:3b0d656f5fff31c7d8a64f500a703dcf3f35e98ce78f602831a73059a5e6a012`<br>`postgres@sha256:3b0d656f5fff31c7d8a64f500a703dcf3f35e98ce78f602831a73059a5e6a012` | `sha256:6ab12ad4395ee49ab49fe19530f7e183c5a9c97fc47cf687b3e281bec5f91ee4` | `9a99ec065edc6ce0cf1fc9e13b9b59a80dc7ebb8f8af1403329f6f12dc73edff` | `2026-06-27T15:57:21.176708466Z` | `2026-06-27T15:57:22.133110621Z` |
| redis | `aether-redis` | `running / healthy` | `redis:7-alpine` | `REDIS_VERSION=7.4.9` | `sha256:6ab0b6e7381779332f97b8ca76193e45b0756f38d4c0dcda72dbb3c32061ab99`<br>`redis@sha256:6ab0b6e7381779332f97b8ca76193e45b0756f38d4c0dcda72dbb3c32061ab99` | `sha256:b1addbe72465a718643cff9e60a58e6df1841e29d6d7d60c9a85d8d72f08d1a7` | `94725b145b2cca2323862fe055245643823b65942c5c0f8d08ce01cc6994b5f2` | `2026-06-17T18:25:42.887791285Z` | `2026-06-27T09:59:59.000240665Z` |


## 2026-07-04 21:47:36 UTC - 数据库迁移 SQL / Backfill 快照

- 记录方式：检查仓库迁移文件，并只读查询 Postgres 元数据表；未执行任何迁移、backfill 或数据库写操作。
- 当前运行数据库：`aether-postgres` / database `aether`。
- 当前栈使用 Postgres；仓库内也存在 MySQL/SQLite 迁移目录，但本快照只记录当前运行 Postgres 的实际应用状态。
- 迁移机制：代码中使用 `sqlx::migrate!("./migrations/postgres")`；Backfill 使用 `sqlx::migrate!("./backfills/postgres")`，执行记录分别在 `public._sqlx_migrations` 与 `public.schema_backfills`。
- 注意：迁移文件会编译/嵌入到应用镜像；真正更新到的新镜像可能包含比当前工作目录更多或不同的迁移，更新后需要再记录一次“更新后快照”。

### Postgres schema migrations

- 本地目录：`crates/aether-data/migrations/postgres`
- 本地迁移 SQL 数量：`46`
- 本地迁移清单 SHA256：`84665d48900c28193befdb098af235e927a8332dfe8379b3c9e73f8f7a01a176`
- 数据库已成功应用：`43`；失败记录：`0`
- 数据库最新已应用：`20260614000000` / `add usage prompt capture entries` / `2026-06-15 00:27:07.022726+08`
- 相对当前工作目录待应用：`3`
| 待应用迁移 SQL | 文件 SHA256 |
|---|---|
| 20260702000000_add_usage_cafecode_uid_filter_index.sql | c4c62f330a5726be6750b568cacf3297eaf68dbb3fd7cf808dbf5b95975af273 |
| 20260702000001_add_usage_cafecode_uname_filter_index.sql | 2a2fd99a235ad5eecb361974c1055e74510272bdf91ec808c23258a69d89cdf6 |
| 20260702000002_add_usage_client_family_filter_index.sql | ec6eea3eebfcc4e34a5cbd58049c0ee44ce879cf5602af26ccc8839093054222 |

<details>
<summary>数据库已应用 migration 列表</summary>


| version | description | success | installed_on | execution_time |
|---|---|---|---|---|
| 20260403000000 | baseline | true | 2026-05-05 18:38:36.812353+08 | 17386065 |
| 20260406000000 | add provider endpoints health score | true | 2026-05-05 18:38:36.830406+08 | 878984 |
| 20260410000000 | add usage token semantics v2 | true | 2026-05-05 18:38:36.83163+08 | 14120248 |
| 20260413020000 | squash usage schema split | true | 2026-05-05 18:38:36.846201+08 | 29842931 |
| 20260413030000 | deprecate usage legacy http columns | true | 2026-05-05 18:38:36.876503+08 | 1222351 |
| 20260415000000 | add redeem codes | true | 2026-05-05 18:38:36.878073+08 | 21159206 |
| 20260418000000 | add usage http audit body states | true | 2026-05-05 18:38:36.899816+08 | 1339587 |
| 20260421000000 | add stats user summary and enrich user aggregates | true | 2026-05-05 18:38:36.901473+08 | 34704459 |
| 20260422110000 | add stats aggregate read path support | true | 2026-05-05 18:38:36.936632+08 | 101758774 |
| 20260422120000 | add api key usage stats | true | 2026-05-05 18:38:37.039174+08 | 2671351 |
| 20260423000000 | fix provider api key api formats nullable | true | 2026-05-05 18:38:37.042209+08 | 2032911 |
| 20260424000000 | add billing v3 settlement snapshot | true | 2026-05-05 18:38:37.044606+08 | 10336082 |
| 20260428000000 | normalize api format aliases | true | 2026-05-05 18:38:37.055316+08 | 28170042 |
| 20260502000000 | add provider key auth channel mismatch formats | true | 2026-05-05 18:38:37.083858+08 | 7195984 |
| 20260505000000 | sync core export columns | true | 2026-05-08 16:56:10.495457+08 | 38860610 |
| 20260505130000 | project upstream stream in usage billing facts | true | 2026-05-08 16:56:10.53529+08 | 12903765 |
| 20260507000000 | cascade announcement reads on delete | true | 2026-05-08 16:56:10.548618+08 | 8973356 |
| 20260507120000 | add management token permissions | true | 2026-05-08 16:56:10.55922+08 | 664640 |
| 20260508000000 | add proxy node metrics | true | 2026-05-16 08:51:13.602084+08 | 31100515 |
| 20260509000000 | add background tasks | true | 2026-05-16 08:51:13.63392+08 | 23645397 |
| 20260509120000 | add user groups | true | 2026-05-16 08:51:13.657983+08 | 28438649 |
| 20260510000000 | normalize management token jsonb | true | 2026-05-16 08:51:13.687035+08 | 19454404 |
| 20260510120000 | normalize empty user policy modes | true | 2026-05-16 08:51:13.70705+08 | 1297777 |
| 20260511000000 | add provider api key pool sort indexes | true | 2026-05-16 08:51:13.70881+08 | 7618264 |
| 20260511120000 | exclude admins from default user group | true | 2026-05-16 08:51:13.717135+08 | 2609297 |
| 20260511130000 | add pool member scores | true | 2026-05-16 08:51:13.720047+08 | 14366716 |
| 20260512000000 | add feature settings | true | 2026-05-16 09:31:46.806933+08 | 7655608 |
| 20260512090000 | add payment gateway plans | true | 2026-05-16 09:31:46.815451+08 | 79302325 |
| 20260512110000 | add billing plan purchase limit scope | true | 2026-05-16 09:31:46.895391+08 | 914907 |
| 20260515000000 | add audit admin to userrole | true | 2026-05-16 09:31:46.896738+08 | 2920687 |
| 20260516000000 | add routing profiles | true | 2026-06-07 10:03:48.087422+08 | 44476251 |
| 20260518000000 | add usage counter deltas | true | 2026-06-07 10:03:48.133053+08 | 128713846 |
| 20260519000000 | add referrals privacy required announcements | true | 2026-06-07 10:03:48.262476+08 | 48567606 |
| 20260519120000 | add oauth provider icon url | true | 2026-06-07 10:03:48.311995+08 | 798052 |
| 20260519130000 | add provider api key default pool sort index | true | 2026-06-07 10:03:48.313136+08 | 3532244 |
| 20260520000000 | add api key ip rules | true | 2026-06-07 10:03:48.317137+08 | 788197 |
| 20260520010000 | preserve usage provider identity | true | 2026-06-07 10:03:48.318284+08 | 259674608 |
| 20260522000000 | decouple usage identity foreign keys | true | 2026-06-07 10:03:48.578453+08 | 4187021 |
| 20260524000000 | add scheduler hotspot indexes | true | 2026-06-07 10:03:48.583595+08 | 24542046 |
| 20260527000000 | add provider key name hotspot index | true | 2026-06-07 10:03:48.609527+08 | 5148589 |
| 20260528000000 | normalize endpoint api roots | true | 2026-06-07 10:03:48.615119+08 | 8784409 |
| 20260528010000 | repair missing routing profiles | true | 2026-06-07 10:03:48.624391+08 | 1883469 |
| 20260614000000 | add usage prompt capture entries | true | 2026-06-15 00:27:07.022726+08 | 1653463 |

</details>


<details>
<summary>本地 Postgres migration SQL 清单 / SHA256</summary>


| migration SQL | 文件 SHA256 |
|---|---|
| 20260403000000_baseline.sql | 885e82570c2959fee6d084511c9ad2018769447eb8d7b0155babdb21ed6e829a |
| 20260406000000_add_provider_endpoints_health_score.sql | 40d4658d84c3b73f0d02758ec59cd1eeae3d5e54420c2a70a4ef72f17b4fc4a7 |
| 20260410000000_add_usage_token_semantics_v2.sql | effb61fb8b6884a811ffd21c58f8ac36fea4957cde0f6293580a6b01926034cf |
| 20260413020000_squash_usage_schema_split.sql | fafff61388dd45ff359d647079d3629801f23810c3ec2b27a844a308f2dac50a |
| 20260413030000_deprecate_usage_legacy_http_columns.sql | 567d7347127989c7bbed7b8a08423583468b840956b1ca21e87da7193539a01d |
| 20260415000000_add_redeem_codes.sql | 5330862f2228a37f2b80fa5668173598f2f676217aaa3cdb3086b1343e9f9baa |
| 20260418000000_add_usage_http_audit_body_states.sql | cb6ecb2c49f823171447dcba12a4f628893a6509a6cc8bd2088d4abb4e0d13a6 |
| 20260421000000_add_stats_user_summary_and_enrich_user_aggregates.sql | 21237bd05bc4ee091cd13ed525c9b540fe33fe6188fa3b96e96a9f37d74868c0 |
| 20260422110000_add_stats_aggregate_read_path_support.sql | 55ea82050d4e62ab2d0dd0dd505fccd8919b9898cfd3e2719053f7cac1cf0f32 |
| 20260422120000_add_api_key_usage_stats.sql | e51c1f71a662db55808e8c47741ee874240e2ecb23e06a6ef649d7111b3502de |
| 20260423000000_fix_provider_api_key_api_formats_nullable.sql | b1c7f45f4f01a06cb34a0f0218124904a67624c46c82e8c184c636e5c35baed7 |
| 20260424000000_add_billing_v3_settlement_snapshot.sql | 38a7ee698ee1efc2923bce6c66842d3db45dffae6eaa35f34124d42d037458aa |
| 20260428000000_normalize_api_format_aliases.sql | b74359fce31b8ff1ad325b5675ed3506e0844552201dd98d3db91fdeffe89d4a |
| 20260502000000_add_provider_key_auth_channel_mismatch_formats.sql | 0ef238e22fa842fb9e097102439901f950c90b933d157015de44ebbec963736b |
| 20260505000000_sync_core_export_columns.sql | e6e0cadd58e278763b2514c3314e14cd38e54c0ef844fc65b0716aad756e1781 |
| 20260505130000_project_upstream_stream_in_usage_billing_facts.sql | cce55cbba145009e7f704651c5febe19c6436c9b8bc29724dcc0499f82c36597 |
| 20260507000000_cascade_announcement_reads_on_delete.sql | d0681339435fd7ad0bd8ea388c29ea8c055c43515f3e8b2aef6bf717b33686e8 |
| 20260507120000_add_management_token_permissions.sql | 1c1c705ce4c0d98d3a5df972ac03d7296b11e2016e7cffde5698bda86f610983 |
| 20260508000000_add_proxy_node_metrics.sql | 5a0ab860f5857ca92fa658df30ca768862aafc0755b75ce51bea77ca36106838 |
| 20260509000000_add_background_tasks.sql | 29fb6a853e8e19a1464b007083f80d68e351ec0a1c6b71ce2c7f7d553dc01c62 |
| 20260509120000_add_user_groups.sql | 4e207e6d8ffa73e82e3853f28dad4bce8cbadccfffdc8bc75051e40259a02901 |
| 20260510000000_normalize_management_token_jsonb.sql | 5f101c3871754a6c748c85759dc6df77a60efa55771ef1a8ea5c1a0c353e015a |
| 20260510120000_normalize_empty_user_policy_modes.sql | 5959b58d5862e7601a1bab18d88e4e7c5f841a5387e63933c63505832f22a94f |
| 20260511000000_add_provider_api_key_pool_sort_indexes.sql | d8cb2fc3b2b6f16e61e79c5eb30e50ee038e23ed63ae20ac0e8b5eb083b0507a |
| 20260511120000_exclude_admins_from_default_user_group.sql | 36276e0c3268ab84efc12c07fdbd3a19417060d17e624c6c2dbc4f69b774f78c |
| 20260511130000_add_pool_member_scores.sql | 8c26f3bf1f06e4cbf37e075ed65e67a4fe2d02adbda0cd71efea6be44b6b3d0a |
| 20260512000000_add_feature_settings.sql | fef2479d883651e1d18b8dc1afbdeb28c3bf418c1d6197ac82d5b3017b4bc1c3 |
| 20260512090000_add_payment_gateway_plans.sql | a8cb0822f935ee066113e81a868acd2bd010d8257133034543562ad8f7c7cbc0 |
| 20260512110000_add_billing_plan_purchase_limit_scope.sql | 16218d3430e693f436d29fc2f9e9e44781ba9c523cd68626fda8162c96ff36de |
| 20260515000000_add_audit_admin_to_userrole.sql | 7d7f24147b5cc97160b83fe08c7a5f2aeb2802446b7f834f40d6420d2024d31b |
| 20260516000000_add_routing_profiles.sql | 3cfa0e4129a04711641bde34d34d883dfa5e971fd7b79ff352e153e9e9adb015 |
| 20260518000000_add_usage_counter_deltas.sql | af93f4d30ec6220686d45ed90ed2642cbb9353de515e8e710ee5ac49c9e65e06 |
| 20260519000000_add_referrals_privacy_required_announcements.sql | 020435bac1467f94ceb23fb84365060f3bb8be3788dcbb48ee803efc00060637 |
| 20260519120000_add_oauth_provider_icon_url.sql | db349139d0158d3d521f03d51c9253e4ffcd5b2da6e6c8b79b69494389265f81 |
| 20260519130000_add_provider_api_key_default_pool_sort_index.sql | 374c2f7777ec6fef908c5741f4f57b9616124a5eda3da6d9e54db4917b901d30 |
| 20260520000000_add_api_key_ip_rules.sql | 52143a4ec2d18057d9cf8e6f300a8c03cc14800b6cc6dbe9da1a73220210fd3b |
| 20260520010000_preserve_usage_provider_identity.sql | 84718698da673b226bba8783f444193fe6347cc432a9dedea4bb7d7a967ffbe7 |
| 20260522000000_decouple_usage_identity_foreign_keys.sql | 1d824d5d0240ecd559202a423c71f7979e891dce513a5db54af7f853abee94e7 |
| 20260524000000_add_scheduler_hotspot_indexes.sql | 7f8911376f5a2869dd6fa462fa0484fd6288bc155ac49c1b923693e2d008ec56 |
| 20260527000000_add_provider_key_name_hotspot_index.sql | fbe159877e8b61a3654bf9807128f80c678cd133ef81bb1013e615d1c87c4df6 |
| 20260528000000_normalize_endpoint_api_roots.sql | 9dedc99a35cf425ae6010ceff17ec476b618f7e6c4b32ffdbf3423d8c5178996 |
| 20260528010000_repair_missing_routing_profiles.sql | d27bf53f331bc75b073dbe8aa5fbca35d32e617e7814eec054338aa767d64fce |
| 20260614000000_add_usage_prompt_capture_entries.sql | 7e6e5413288f817e1cbc95a60dad16b4709e340ba8b945b12a376d941e65441a |
| 20260702000000_add_usage_cafecode_uid_filter_index.sql | c4c62f330a5726be6750b568cacf3297eaf68dbb3fd7cf808dbf5b95975af273 |
| 20260702000001_add_usage_cafecode_uname_filter_index.sql | 2a2fd99a235ad5eecb361974c1055e74510272bdf91ec808c23258a69d89cdf6 |
| 20260702000002_add_usage_client_family_filter_index.sql | ec6eea3eebfcc4e34a5cbd58049c0ee44ce879cf5602af26ccc8839093054222 |

</details>


### Postgres backfills

- 本地目录：`crates/aether-data/backfills/postgres`
- 本地 backfill SQL 数量：`5`
- 本地 backfill 清单 SHA256：`582f030a6f140c0f320dc58f7fce11c6d136a404971ce118e17c88ed9aa7e507`
- 数据库已成功应用：`5`；失败记录：`0`
- 数据库最新已应用：`20260616120000` / `backfill usage risk control flag` / `2026-06-16 09:48:40.028043+08`
- 相对当前工作目录待应用：`0`
| 待应用 backfill SQL | 文件 SHA256 |
|---|---|
| 无 | - |

<details>
<summary>数据库已应用 backfill 列表</summary>


| version | description | success | applied_at | execution_time |
|---|---|---|---|---|
| 20260422110000 | backfill stats aggregate read path support | true | 2026-05-05 18:38:37.09485+08 | 795 |
| 20260422120000 | backfill api key usage stats | true | 2026-05-05 18:38:37.900889+08 | 1 |
| 20260504120000 | rebuild cost savings input price | true | 2026-05-08 16:56:10.569263+08 | 2681 |
| 20260505120000 | rebuild global model usage count | true | 2026-05-08 16:56:13.257926+08 | 138 |
| 20260616120000 | backfill usage risk control flag | true | 2026-06-16 09:48:40.028043+08 | 6507 |

</details>


<details>
<summary>本地 Postgres backfill SQL 清单 / SHA256</summary>


| backfill SQL | 文件 SHA256 |
|---|---|
| 20260422110000_backfill_stats_aggregate_read_path_support.sql | 8c5b902d1989b55ac348a5d505aa4f196f13ffeebad9a358810da8b41e5cbd90 |
| 20260422120000_backfill_api_key_usage_stats.sql | 39708bbb78c1151b5a3f64d5c483236411d8a81e97dafe98ee700e078e6a07d5 |
| 20260504120000_rebuild_cost_savings_input_price.sql | 4f6fc98c60da00f0f9ac9b37a61b4178b18bafb2e807081f0aa7011e48e09afe |
| 20260505120000_rebuild_global_model_usage_count.sql | 62a038957109813850f54bce8619cef9e1e76e043eeeff96aa11935e80fbd70a |
| 20260616120000_backfill_usage_risk_control_flag.sql | 797054ccf530da8eec7e36db76991719bb36e2780c8e80d442a08bb4bac563f6 |

</details>


## 2026-07-04 23:41:41 UTC - 更新准备 / 目标版本确认

- 目标版本 tag：`backend-v0.7.13`
- 目标提交：`21802d95e746eab6bcb526c8c1e336e1c7f9e21b`
- GitHub Actions：`Release Aether` 已完成，未包含 macOS 构建任务。
- 目标镜像：`ghcr.io/shirtiny/aether:backend-v0.7.13`
- 目标镜像 index digest：`sha256:b5bf2a17cd17cb1a3c7cb25a6d0dedb5e9194b58fd6c5cf1faa902221f49c086`
- 不可变镜像引用：`ghcr.io/shirtiny/aether@sha256:b5bf2a17cd17cb1a3c7cb25a6d0dedb5e9194b58fd6c5cf1faa902221f49c086`
- `latest` 当前也指向同一个 index digest：`sha256:b5bf2a17cd17cb1a3c7cb25a6d0dedb5e9194b58fd6c5cf1faa902221f49c086`

### 当前运行版本（更新前）

- 当前 app 容器镜像标签：`ghcr.io/shirtiny/aether:latest`
- 当前 app 容器镜像 ID：`sha256:f1970edb3276a120d2ecef85300f277eb949ac1285fc270b04829dc41a7d2315`
- 当前 app revision：`fc4e73f0ed275cf6be70f85dcd92b5dbb30df09f`
- 当前 app manifest digest：`sha256:ba984d1f4ba6bef94f5b89f4ee3d9bea1e8afbe75e23e68aa4375e8b4c56aa0c`
- 当前容器状态：`aether-app` / `Up` / `healthy`
- `.env` 当前未设置 `APP_IMAGE=`，Compose 会使用 `docker-compose.yml` 默认值 `ghcr.io/shirtiny/aether:latest`。

### 推荐更新方式

优先使用不可变 digest 固定版本：

```env
APP_IMAGE=ghcr.io/shirtiny/aether@sha256:b5bf2a17cd17cb1a3c7cb25a6d0dedb5e9194b58fd6c5cf1faa902221f49c086
```

然后由操作者明确执行生产更新命令。若使用现有 `latest` 流程，当前 `latest` 已解析到同一目标 digest，但仍建议更新后记录实际运行 digest。

本条仅记录目标版本和准备状态；未执行 `pull`、`up`、`restart`、`update.sh` 或任何生产生命周期操作。


## 2026-07-04 23:57:01 UTC - 更新完成 / 更新后快照

- 授权说明：用户已在本轮明确授权执行生产更新，并要求“开始更新版本 并时刻盯着容器状态”。
- 更新范围：仅更新 `app` 服务；`postgres`、`redis` 未重建、未拉取、未重启。
- 目标版本 tag：`backend-v0.7.13`
- 目标提交：`21802d95e746eab6bcb526c8c1e336e1c7f9e21b`
- 目标镜像不可变引用：`ghcr.io/shirtiny/aether@sha256:b5bf2a17cd17cb1a3c7cb25a6d0dedb5e9194b58fd6c5cf1faa902221f49c086`
- `.env` 更新前备份：`.env.backup_20260704235245_pre_backend_v0.7.13`
- `.env` 当前固定：

```env
APP_IMAGE=ghcr.io/shirtiny/aether@sha256:b5bf2a17cd17cb1a3c7cb25a6d0dedb5e9194b58fd6c5cf1faa902221f49c086
```

### 本次执行动作

1. 记录更新前 `app` 容器镜像、健康状态、迁移/backfill 状态。
2. 备份 `.env` 到 `.env.backup_20260704235245_pre_backend_v0.7.13`。
3. 将 `.env` 的 `APP_IMAGE` 固定为 `backend-v0.7.13` 对应的不可变 digest。
4. 执行 `docker compose pull app`。
5. 执行 `docker compose up -d app`。
6. 持续检查 `docker compose ps`、`docker inspect`、`/_gateway/health`、应用日志、迁移/backfill 元数据。

### 容器版本变化

| 项目 | 更新前 | 更新后 |
|---|---|---|
| app 镜像引用 | `ghcr.io/shirtiny/aether:latest` | `ghcr.io/shirtiny/aether@sha256:b5bf2a17cd17cb1a3c7cb25a6d0dedb5e9194b58fd6c5cf1faa902221f49c086` |
| app 版本标签 | `latest` | `backend-v0.7.13` |
| app revision | `fc4e73f0ed275cf6be70f85dcd92b5dbb30df09f` | `21802d95e746eab6bcb526c8c1e336e1c7f9e21b` |
| app 运行镜像 ID | `sha256:f1970edb3276a120d2ecef85300f277eb949ac1285fc270b04829dc41a7d2315` | `sha256:b5bf2a17cd17cb1a3c7cb25a6d0dedb5e9194b58fd6c5cf1faa902221f49c086` |
| app manifest digest | `sha256:ba984d1f4ba6bef94f5b89f4ee3d9bea1e8afbe75e23e68aa4375e8b4c56aa0c` | 目标 index digest：`sha256:b5bf2a17cd17cb1a3c7cb25a6d0dedb5e9194b58fd6c5cf1faa902221f49c086` |
| app 容器 ID | `1c804e90a03a69cd4ac4af78f00174281443b6f9eecc3b715a86eb14054f43c9` | `38bbb095797b27d3d7922011c8db6bc70ccd40a135693ae4299e6160113ff85f` |
| app 启动时间 | `2026-07-03T03:13:22.64022712Z` | `2026-07-04T23:53:01.650429031Z` |
| app 状态 | `running / healthy` | `running / healthy` |
| app 重启次数 | 未在该条记录中统计 | `0` |

### 更新后容器状态

| 服务 | 镜像 | 状态 | 备注 |
|---|---|---|---|
| app | `ghcr.io/shirtiny/aether@sha256:b5bf2a17cd17cb1a3c7cb25a6d0dedb5e9194b58fd6c5cf1faa902221f49c086` | `Up / healthy` | 新容器 `38bbb095797b...`，重启次数 `0` |
| postgres | `postgres:15` | `Up / healthy` | 未重建 |
| redis | `redis:7-alpine` | `Up / healthy` | 未重建 |

监控窗口：`2026-07-04T23:55:00Z`、`23:55:30Z`、`23:56:00Z`、`23:56:31Z`、`23:57:01Z`、`23:58:05Z` 多次采样均显示 `app` / `postgres` / `redis` 为 healthy，`app` 重启次数保持 `0`，`/_gateway/health` 返回：

```json
{"status":"ok","component":"aether-gateway","control_api_enabled":true,"request_concurrency":null,"distributed_request_concurrency":null}
```

`2026-07-04T23:58:45Z` 只读扫描更新后最近 5 分钟应用日志，未发现 `panic` / `fatal` / `ERROR` / 数据库准备失败类严重日志。

### 更新后迁移 / Backfill 状态

- `app` 启动日志显示自动数据库准备已启用，并在启动期间执行了 `3` 条待应用 migration。
- 更新前 schema migrations：成功 `43`，失败 `0`，最新 `20260614000000`。
- 更新后 schema migrations：成功 `46`，失败 `0`，最新 `20260702000002`。
- 更新前后 backfills：成功 `5`，失败 `0`，最新均为 `20260616120000`；本次未新增 backfill。

本次新增成功应用的 migrations：

| version | description | success | installed_on | execution_time |
|---|---|---|---|---|
| 20260702000000 | add usage cafecode uid filter index | true | 2026-07-05 07:53:03.788226+08 | 1825140572 |
| 20260702000001 | add usage cafecode uname filter index | true | 2026-07-05 07:53:05.638396+08 | 1849364959 |
| 20260702000002 | add usage client family filter index | true | 2026-07-05 07:53:07.456695+08 | 1817874862 |

最新 backfill 记录保持不变：

| version | description | success | applied_at | execution_time |
|---|---|---|---|---|
| 20260616120000 | backfill usage risk control flag | true | 2026-06-16 09:48:40.028043+08 | 6507 |


## 2026-07-26 02:24:08 UTC - 更新完成 / backend-v0.7.52

- 授权说明：用户本轮明确要求“更新两个线上容器，注意遵守更新规范和流程”。
- 更新范围：仅更新 `app` 服务；`postgres`、`redis` 未拉取、未重建、未重启（保持 Up 4 weeks / healthy）。
- 上线顺序：本次先更新 Aether，再更新 Sub2API（`cafecode-v0.0.32` → `cafecode-v0.0.34`），与 Sub2API 新增 alpha search 路由依赖 Aether standalone search surface 的方向一致。
- 目标版本 tag：`backend-v0.7.52`（`custom` 分支 HEAD 与 tag 同点）
- 目标提交：`e1343300e776873b90287c737620e45827dda5e0`
- GitHub Actions：`Release Aether` #121 @ `backend-v0.7.52` 结论 success。
- 目标镜像不可变引用：`ghcr.io/shirtiny/aether@sha256:95fcce7d246b662826a7f6b9436541cc70a43ec166a4f54e825ddf598a465140`（digest 已与 registry manifest 交叉核对一致）。
- `.env` 更新前备份：`.env.backup_20260726T022307Z_pre_backend_v0.7.52`（SHA256 `966e734a83876eeba170d044fad87365915a1ad54a0047a6ce62bc90a3508fc2`）。
- `.env` 当前固定：

```env
APP_IMAGE=ghcr.io/shirtiny/aether@sha256:95fcce7d246b662826a7f6b9436541cc70a43ec166a4f54e825ddf598a465140
```

### 本次执行动作

1. 记录更新前 `app` 容器镜像、健康状态、迁移/backfill 状态。
2. 校验 `backend-v0.7.46..backend-v0.7.52` 的 `crates/aether-data/migrations`、`backfills` 无任何文件变化。
3. 备份 `.env` 并将 `APP_IMAGE` 固定为 `backend-v0.7.52` 对应的不可变 digest。
4. `docker pull` 预拉取镜像并校验 label 版本/revision。
5. 执行 `docker compose up -d --no-deps app`（`--no-deps` 确保不触碰 postgres/redis）。
6. 检查 `docker compose ps`、`docker inspect`、`/_gateway/health`、应用日志、迁移/backfill 元数据。

### 容器版本变化

| 项目 | 更新前 | 更新后 |
|---|---|---|
| app 镜像引用 | `ghcr.io/shirtiny/aether@sha256:5c5666d4c52b0ce8e7c016d1d35af5ccadd2ebfebbecf14f9f2544eeedc13ecc` | `ghcr.io/shirtiny/aether@sha256:95fcce7d246b662826a7f6b9436541cc70a43ec166a4f54e825ddf598a465140` |
| app 版本标签 | `backend-v0.7.46` | `backend-v0.7.52` |
| app revision | `4a57f39befb6288c7f0c5ead128f59f328c39a81` | `e1343300e776873b90287c737620e45827dda5e0` |
| app 容器 ID | `a0a8996c02f4...` | `c133d4fb1b9d...` |
| app 启动时间 | `2026-07-20T05:21:20Z` | `2026-07-26T02:23:52Z` |
| app 状态 | `running / healthy` | `running / healthy` |
| app 重启次数 | `0` | `0` |
| 端口映射 | `127.0.0.1:20123 -> 80/tcp` | 不变 |
| 网络 | `aether_default`、`cpa-shared` | 不变 |

### 更新后验证

- `/_gateway/health` 返回：

```json
{"status":"ok","component":"aether-gateway","control_api_enabled":true,"request_concurrency":null,"distributed_request_concurrency":null}
```

- 更新后 5 分钟日志扫描 `panic` / `fatal` / `ERROR` 匹配 0 条。
- 实际线上流量核验：更新后已观察到来自 Sub2API 的 `/v1/responses`、`/v1/chat/completions` 请求 `status_code=200` 正常完成。

### 更新后迁移 / Backfill 状态

- schema migrations：更新前后均为成功 `53`、失败 `0`，最新 `20260715000000`，本次无新增迁移。
- backfills：更新前后均为成功 `5`、失败 `0`，最新 `20260616120000`，本次无新增 backfill。

### 回滚参考

- 回滚镜像：`ghcr.io/shirtiny/aether@sha256:5c5666d4c52b0ce8e7c016d1d35af5ccadd2ebfebbecf14f9f2544eeedc13ecc`（`backend-v0.7.46`），如需回滚须另行明确授权。


## 2026-07-26 02:51:19 UTC - 数据库索引清理（选号链路延迟优化尝试）

- 授权：用户要求"你来优化并测试"，针对 aether 选号链路 p50=135ms 的前置耗时。
- 变更范围：仅删除 3 个从未被读取的索引，未改 aether 代码、未重启任何容器、未动数据。

### 执行的 DDL

```sql
DROP INDEX CONCURRENTLY ix_provider_api_keys_circuit_breaker_by_format;  -- 13 MB, GIN, idx_scan=0
DROP INDEX CONCURRENTLY ix_provider_api_keys_health_by_format;           -- 9128 kB, GIN, idx_scan=0
DROP INDEX CONCURRENTLY pool_member_scores_rank_idx;                     -- 2736 kB, btree, idx_scan=0
```

判定依据：`pg_stat_database.stats_reset` 为 NULL（计数器从未重置），同表 `ix_provider_api_keys_id` 同期有 4983 万次扫描，证明计数器正常工作；代码中无任何 `@>` / `?` 操作符使用这两个 JSONB 列。

### 回滚 DDL（如需）

```sql
CREATE INDEX CONCURRENTLY ix_provider_api_keys_circuit_breaker_by_format ON public.provider_api_keys USING gin (circuit_breaker_by_format);
CREATE INDEX CONCURRENTLY ix_provider_api_keys_health_by_format ON public.provider_api_keys USING gin (health_by_format);
CREATE INDEX CONCURRENTLY pool_member_scores_rank_idx ON public.pool_member_scores USING btree (pool_kind, pool_id, capability, scope_kind, scope_id, hard_state, score DESC);
```

### 体积变化

| 表 | 删除前 | 删除后 |
|---|---|---|
| `provider_api_keys`（92 行，2841142 次 update） | 24 MB（索引 23 MB） | 1320 kB（索引 480 kB） |
| `pool_member_scores`（36 行，130 万次 update） | 9584 kB | 6824 kB |

### 延迟测量结果：无改善

测量口径：aether 日志 `auth_context_resolved` → `direct request prepared` 的时间差分布。

| | n | min | p50 | p75 | p90 | p99 | max | mean |
|---|---|---|---|---|---|---|---|---|
| 删除前（02:23–02:51 UTC） | 447 | 59 | **127** | 149 | 171 | 212 | 256 | **131** |
| 删除后（02:51–03:05 UTC） | 333 | 38 | **128** | 154 | 177 | 215 | 246 | **131** |

结论：GIN 索引写放大假设**不成立**，延迟分布无统计差异。索引清理本身仍是净收益（回收 25 MB、消除两张最热写表的写放大与 autovacuum 负担），但不解决延迟问题。

### 真实根因（本次定位到，未修复）

通过 `pg_stat_database.xact_commit` 与网关请求数的两点回归：

- 样本 1：123 秒 / 9281 事务 / 33 请求 → 75.5 xact/s @ 0.275 req/s
- 样本 2：90 秒 / 8756 事务 / 36 请求 → 97.3 xact/s @ 0.400 req/s
- 拟合：**约 174 次 DB 事务/请求**，背景负载约 28 xact/s

按本地容器网络单次往返约 0.5ms 估算，174 次 ≈ 87ms，与观测窗口（min 38 / p50 128ms）量级吻合。**瓶颈是选号链路的 DB 往返次数，不是任何单条慢查询**（DB 缓存命中率 99.92%，41 连接仅 1 活跃，无等待事件）。

代码定位：`apps/aether-gateway/src/ai_serving/planner/candidate_materialization.rs:366-370` 定义了 `LocalCandidateTransportReadMode::{PerCandidate, BatchedPage}`，`BatchedPage` 分支已实现（:812、:1029），但所有调用点均硬编码 `PerCandidate`（:762、:2882、:3008，及 `standard/openai/responses/decision/support.rs:362`），**无配置或环境变量可切换**。

即：修复需要改 aether 代码，数据/配置层无可用杠杆。

### 服务影响

全程无影响。DDL 使用 CONCURRENTLY 未取排他锁，`aether-app` / `sub2api` 保持 healthy、重启次数 0，删除后日志 `panic`/`fatal`/`ERROR` 匹配 0 条，请求持续正常完成。


## 2026-08-01 06:11:29 UTC - 更新完成 / backend-v0.7.64

- 授权说明：用户本轮明确指示“开始更新”，更新完成后要求“补”记录本条。
- 更新范围：仅更新 `app` 服务；`postgres`、`redis` 未拉取、未重建、未重启（`docker compose up -d --no-deps app`）。
- 记录补充：本 doc 上一条 app 更新条目为 `backend-v0.7.52`，但更新前实际运行版本为 `backend-v0.7.61`（`2026-07-26T15:06` 构建）——`v0.7.52 → v0.7.61` 那一步未在本 doc 记录，本条以实际运行的 `v0.7.61` 作为更新前基线。
- 目标版本 tag：`backend-v0.7.64`（`custom` 分支 HEAD 与 tag 同点）
- 目标提交：`e9eed8e3df4473f03126ad96822d058cdf96a5fc`
- GitHub Actions：`Release Aether` @ `backend-v0.7.64` 已完成（本机无 `gh`，通过 ghcr 交叉核对确认：`backend-v0.7.64`、minor tag `0.7`、`latest` 指向同一 manifest，说明测试→前端→构建→推送→retag 整条流水线成功，非半截构建）。
- 目标镜像不可变引用（amd64 平台 manifest digest，沿用既有 pin 约定）：`ghcr.io/shirtiny/aether@sha256:566afc0478e8b38087b1cb11cf1cfa4a7c0ec5fcaa34a5fb7c515237cc9e551e`
- `.env` 更新前备份：`.env.bak.20260801_060903_pre_v0.7.64`（SHA256 `c0858c1058dd17b7f3c7698968c9bd4cca3e85288eadcaa27bb8457ff3a8a567`）。
- `.env` 当前固定：

```env
APP_IMAGE=ghcr.io/shirtiny/aether@sha256:566afc0478e8b38087b1cb11cf1cfa4a7c0ec5fcaa34a5fb7c515237cc9e551e
```

### 本次执行动作

1. 记录更新前 `app` 容器镜像、健康状态、迁移/backfill 状态。
2. 校验 `220e95ebc..e9eed8e3d`（`v0.7.61 → v0.7.64`，11 个提交）的 `crates/aether-data/migrations`、`backfills` 无任何文件变化（`git diff --name-only` 无 migration/schema/sql 命中）。
3. 备份 `.env` 并将 `APP_IMAGE` 固定为 `backend-v0.7.64` 对应的 amd64 平台 manifest digest。
4. `docker compose pull app` 预拉取镜像并校验 label 版本/revision。
5. 执行 `docker compose up -d --no-deps app`（`--no-deps` 确保不触碰 postgres/redis）。
6. 检查 `docker compose ps`、`docker inspect`、`/_gateway/health`、应用日志、迁移/backfill 元数据、线上实时流量。

### 容器版本变化

| 项目 | 更新前 | 更新后 |
|---|---|---|
| app 镜像引用 | `ghcr.io/shirtiny/aether@sha256:3c177bec907f543af3c2049599def1c2a13381d4c76746f250422dee1f27816b` | `ghcr.io/shirtiny/aether@sha256:566afc0478e8b38087b1cb11cf1cfa4a7c0ec5fcaa34a5fb7c515237cc9e551e` |
| app 版本标签 | `backend-v0.7.61` | `backend-v0.7.64` |
| app revision | `220e95ebc3a433d0350cbe699c5981bcf37da400` | `e9eed8e3df4473f03126ad96822d058cdf96a5fc` |
| app 容器 ID | `未捕获（重建前未记录）` | `87a7c82101103ad82d88bf78e89f69dcc2c17e379d3c2f4d850c5c65e69361c0` |
| app 启动时间 | — | `2026-08-01T06:09:32.11369279Z` |
| app 状态 | `running / healthy` | `running / healthy` |
| app 重启次数 | — | `0` |
| 端口映射 | `127.0.0.1:20123 -> 80/tcp` | 不变 |
| 网络 | `aether_default`、`cpa-shared` | 不变 |

### 本次上线内容摘要（11 个提交）

- **feat(grok)** `b90892e7a`：Grok 号池额度不再从 grok.com rate-limit header 推算，改为从官方 xAI billing 接口派生（新增 `docs/grok-billing-quota.md`，前端 `providerKeyQuota.ts` 配套）。**行为变更，需观察。**
- **Codex WebSocket catalog fence 加固**（`2786588a1` `434f18e4d` `bc381847e` `be04f8d28` `e9eed8e3d`）：catalog 栅栏改为强制、修 fence 竞态、收敛 WS handshake header、Codex WS 上游失败单独分类。
- **session 风控拦截**（`2ad8cd8ab` `a683938b8` `ca454be30`）：从“仅告警”改为**真正 enforce 拦截** + 加固 + 措辞澄清。**行为变更，需观察是否误拦。**
- **fix(formats)** `9d7a8a1cc`：发往 OpenAI 目标的每个 inline file payload 都带文件名。
- **docs** `b1bfd8be7`：记录 gateway latency 排查，无运行时影响。

### 更新后验证

- `/_gateway/health` 返回：

```json
{"status":"ok","component":"aether-gateway","control_api_enabled":true,"request_concurrency":null,"distributed_request_concurrency":null}
```

- 健康检查首次探测即 `healthy`，重启次数 `0`，退出码 `0`。
- 更新后日志扫描 `panic` / `fatal` / `ERROR` 匹配 0 条。
- 实际线上流量核验：更新后已观察到 `/v1/responses` 请求 `status_code=200` 正常完成（codex → `chatgpt.com`、`cli-proxy-api` 上游均通）。

### 更新后迁移 / Backfill 状态

- schema migrations：更新前后均为成功 `53`、失败 `0`，最新 `20260715000000`（`grok chat proxy base url`），本次无新增迁移。
- backfills：更新前后均为成功 `5`、失败 `0`，最新 `20260616120000`，本次无新增 backfill。

### 上线后需观察

- Grok 额度改走 xAI 账单口径——核对号池 Grok key 额度显示与调度是否正常。
- session 风控从告警转为强制拦截——注意是否有正常 session 被误拦。

### 回滚参考

- 回滚镜像：`ghcr.io/shirtiny/aether@sha256:3c177bec907f543af3c2049599def1c2a13381d4c76746f250422dee1f27816b`（`backend-v0.7.61`）；操作：改回 `.env` 的 `APP_IMAGE` 后 `docker compose up -d --no-deps app`。如需回滚须另行明确授权。


## 2026-08-01 08:12:32 UTC - 更新完成 / backend-v0.7.65

- 授权说明：用户本轮指示“push tag”，随后在选项中明确选择“监控构建并自动部署”。本条为该授权下的自动部署。
- 更新范围：仅更新 `app` 服务；`postgres`、`redis` 未拉取、未重建、未重启（`docker compose up -d --no-deps app`）。
- 本次内容：**仅一个前端提交** `af6c7878f`（`feat(frontend): add per-account quota refresh to pool list`）——号池列表每个账号行新增单账号「刷新额度」按钮（桌面表格 + 移动两套布局），复用现有 `refresh-quota` 单 key_id 调用、`applyQuotaRefreshResultToCurrentPage` 原地更新与 5 分钟手动冷却，仅对支持配额刷新的 provider 显示。**无后端/迁移改动。**
- 发布链路：本机 commit → push `origin/custom` → 打 tag `backend-v0.7.65`（指向 `af6c7878f`）→ push tag 触发 `release.yml`。构建约 29 分钟；ghcr 交叉核对 `backend-v0.7.65`/`0.7`/`latest` 同一 amd64 digest，确认整条流水线成功。
- 目标镜像不可变引用（amd64 平台 manifest digest）：`ghcr.io/shirtiny/aether@sha256:4befb75fbda14acff2039bd42fbb496c775a8bc34fd7d961407c65f6acb711d3`
- `.env` 更新前备份：`.env.bak.20260801_081053_pre_v0.7.65`。
- `.env` 当前固定：

```env
APP_IMAGE=ghcr.io/shirtiny/aether@sha256:4befb75fbda14acff2039bd42fbb496c775a8bc34fd7d961407c65f6acb711d3
```

### 容器版本变化

| 项目 | 更新前 | 更新后 |
|---|---|---|
| app 镜像引用 | `ghcr.io/shirtiny/aether@sha256:566afc0478e8b38087b1cb11cf1cfa4a7c0ec5fcaa34a5fb7c515237cc9e551e` | `ghcr.io/shirtiny/aether@sha256:4befb75fbda14acff2039bd42fbb496c775a8bc34fd7d961407c65f6acb711d3` |
| app 版本标签 | `backend-v0.7.64` | `backend-v0.7.65` |
| app revision | `e9eed8e3df4473f03126ad96822d058cdf96a5fc` | `af6c7878fb53cbaf6ea94aa28d0e6eb386ccf9d3` |
| app 容器 ID | `87a7c82101103...` | `bb4e9777c7ee11adb38f0fba19b3086cb7d10f1de4bab12aad7214916ac77de3` |
| app 启动时间 | — | `2026-08-01T08:11:19.23363136Z` |
| app 状态 | `running / healthy` | `running / healthy` |
| app 重启次数 | `0` | `0` |
| 端口映射 / 网络 | `127.0.0.1:20123 -> 80/tcp`、`aether_default`、`cpa-shared` | 不变 |

### 更新后验证

- 健康检查首次探测即 `healthy`，重启次数 `0`，退出码 `0`。
- 更新后日志扫描 `panic` / `fatal` / `ERROR` 匹配 0 条。
- 前端已含本次改动：容器内 `PoolManagement-wFwymrik.js` 含 `refresh_quota`（新按钮），`billing_weekly`（billing 显示）在共享 chunk 中。
- `/_gateway/health` 返回 `status: ok`。

### 迁移 / Backfill 状态

- schema migrations：更新前后均为成功 `53`、失败 `0`，最新 `20260715000000`，本次无新增迁移（前端-only）。

### 回滚参考

- 回滚镜像：`ghcr.io/shirtiny/aether@sha256:566afc0478e8b38087b1cb11cf1cfa4a7c0ec5fcaa34a5fb7c515237cc9e551e`（`backend-v0.7.64`）；操作：改回 `.env` 的 `APP_IMAGE` 后 `docker compose up -d --no-deps app`。如需回滚须另行明确授权。


## 2026-08-01 08:58:02 UTC - 更新完成 / backend-v0.7.66

- 授权说明：用户在选项中明确选择“只发前端修复 v0.7.66（监控自动部署）”。本条为该授权下的自动部署。
- 更新范围：仅更新 `app` 服务；`postgres`、`redis` 未拉取、未重建、未重启（`docker compose up -d --no-deps app`）。
- 本次内容：**仅一个前端提交** `c7415a5ce`（`fix(frontend): surface grok billing windows in pool list meters`）。修复 grok 号池列表**进度条**不显示 billing 的遗漏：`buildQuotaProgressItemsFromSnapshot` 的 grok 分支原本只用 `requests`/`tokens` 窗口且 `dimensionItems.length > 0` 即提前 return，导致付费号权威 billing（`billing_weekly`/`billing_monthly`）永远不显示、卡在静态天花板 8300/8300。改为优先构建 `周`/`月` billing 进度条（周百分比 + 月 USD `$X/$Y`），有权威 billing 即返回；Free 号无 billing 时仍回落 requests/tokens。**无后端/迁移改动。**
- 发布链路：commit → push `origin/custom` → tag `backend-v0.7.66`（指向 `c7415a5ce`）→ push tag 触发 `release.yml`。构建约 34 分钟；ghcr 交叉核对 `backend-v0.7.66`/`0.7`/`latest` 同一 amd64 digest。
- 目标镜像不可变引用（amd64 平台 manifest digest）：`ghcr.io/shirtiny/aether@sha256:895c04b0680009e391b57492631e884f66474e2142cc81a671c10cacd56fb232`
- `.env` 更新前备份：`.env.bak.20260801_085706_pre_v0.7.66`。
- 注意（记录备查）：本次部署时工作区存在 **17 个未提交的后端 rust 改动**（usage-runtime / execution_runtime 重构，mtime 08:11–08:16 UTC，非本次操作产生），**未纳入本次发布**——tag 只含 `c7415a5ce` 这一个前端提交，后端改动不进镜像。来源需另行确认。

### 容器版本变化

| 项目 | 更新前 | 更新后 |
|---|---|---|
| app 镜像引用 | `…@sha256:4befb75fbda14acff2039bd42fbb496c775a8bc34fd7d961407c65f6acb711d3` | `…@sha256:895c04b0680009e391b57492631e884f66474e2142cc81a671c10cacd56fb232` |
| app 版本标签 | `backend-v0.7.65` | `backend-v0.7.66` |
| app revision | `af6c7878fb53cbaf6ea94aa28d0e6eb386ccf9d3` | `c7415a5ce1330b654ed137ce37cfa2a44e8958c7` |
| app 容器 ID | `bb4e9777c7ee…` | `4824b567ab1aa909486a0c1a9372c924f2d18fd4b4ebe31bd61f3fcc2ea0ddba` |
| app 启动时间 | — | `2026-08-01T08:57:30.22230382Z` |
| app 状态 / 重启次数 | `running / healthy` / `0` | `running / healthy` / `0` |
| 端口 / 网络 | `127.0.0.1:20123 -> 80/tcp`、`aether_default`、`cpa-shared` | 不变 |

### 更新后验证

- 健康检查首次探测即 `healthy`，重启次数 `0`，退出码 `0`。
- 更新后日志扫描 `panic` / `fatal` / `ERROR` 匹配 0 条。
- 修复已含在镜像：容器内 `PoolManagement-ChW1JR0d.js` 含 `billing_weekly`（进度条读 billing 窗口）。
- schema migrations：更新前后均为成功 `53`、失败 `0`，最新 `20260715000000`，本次无新增迁移（前端-only）。

### 回滚参考

- 回滚镜像：`ghcr.io/shirtiny/aether@sha256:4befb75fbda14acff2039bd42fbb496c775a8bc34fd7d961407c65f6acb711d3`（`backend-v0.7.65`）；操作：改回 `.env` 的 `APP_IMAGE` 后 `docker compose up -d --no-deps app`。如需回滚须另行明确授权。

## 2026-08-09 15:52:32 UTC - 更新前快照 / backend-v0.7.71

- 授权说明：用户明确要求“更新线上容器”。本次仅按授权更新 Aether `app` 服务，不更新数据库或 Redis 基础镜像。
- 目标发布已确认：Git tag `backend-v0.7.71`，commit `2747cad089d647340c1a961cb4512f4b417b1c3e`；GitHub Actions `Release Aether` run `31320178650` 已成功；目标 amd64 镜像不可变引用：`ghcr.io/shirtiny/aether@sha256:4e9e2693edb221d99f54f6c5307dd737035756fb727d1e67571316446ffdd9fb`。
- 更新范围：compose project `aether`，目录 `/opt/stacks/aether`，文件 `/opt/stacks/aether/docker-compose.yml`，服务 `app`；`postgres`、`redis` 不在本次更新范围。
- **截至本快照尚未执行任何线上更新、重启、重建或镜像拉取。**

### 当前容器

| 容器 / 服务 | 状态 | 镜像配置 | 镜像 ID / manifest digest | 重启次数 | 创建时间 | 启动时间 |
|---|---|---|---|---:|---|---|
| `aether-app` / `app` | running / healthy | `ghcr.io/shirtiny/aether@sha256:16e04839df6d7eea032ea682e97515d0e8fb4da0e8f0970bf59d7980615a3a01` | `sha256:16e04839df6d7eea032ea682e97515d0e8fb4da0e8f0970bf59d7980615a3a01` | 0 | `2026-08-09T01:27:19.892955864Z` | `2026-08-09T01:27:31.886617523Z` |
| `aether-postgres` / `postgres` | running / healthy | `postgres:15` | `sha256:3b0d656f5fff31c7d8a64f500a703dcf3f35e98ce78f602831a73059a5e6a012` | 0 | `2026-06-27T15:57:21.176708466Z` | `2026-06-27T15:57:22.133110621Z` |
| `aether-redis` / `redis` | running / healthy | `redis:7-alpine` | `sha256:6ab0b6e7381779332f97b8ca76193e45b0756f38d4c0dcda72dbb3c32061ab99` | 0 | `2026-06-17T18:25:42.887791285Z` | `2026-06-27T09:59:59.000240665Z` |

- app OCI labels：version `backend-v0.7.70`，revision `1cb846fe208dc6829a6b9bfc670e0d631384bdb`；容器 ID `7ed6b14897bee2220fb50a61104f536f735bc27c63a904c87215bf8f3a9b3f95`。
- app 端口 / 网络：`127.0.0.1:20123 -> 80/tcp`；`aether_default`、`cpa-shared`。

### 数据库迁移 / Backfill 状态

- 数据库：`aether`（PostgreSQL 15），数据库大小约 `2335 MB`；迁移表 `public._sqlx_migrations`：总数 `53`，成功 `53`，失败 `0`；最新成功迁移 `20260715000000_grok_chat_proxy_base_url.sql`，应用时间 `2026-07-18 23:39:25.268021+08`。
- Backfill 表 `public.schema_backfills`：总数 `5`，成功 `5`，失败 `0`；最新成功 backfill `20260616120000_backfill_usage_risk_control_flag.sql`，应用时间 `2026-06-16 09:48:40.028043+08`。
- 发布记录与当前工作树迁移清单均为 `53` 个，迁移 manifest SHA256 `72662063e1e76c20b71baf4d250d296e9b1625d3b8cfbd1c025de9cae06e1868`；backfill 均为 `5` 个，manifest SHA256 `34b5daa82eeed24b97172979b17df720f36d412914930ffb78f01037991a471e`。相对目标发布 pending migrations/backfills：`0 / 0`，无失败项；本次不预期执行 schema/data 变更。
- `.env` 当前 SHA256：`017b22157b4352eedda4bc446008a0dbfeb387c6cfea15b2fe350b7b76dd7e85`；其中 `APP_IMAGE` 仍指向上述 v0.7.70 digest。将在备份后仅替换该变量。
- 数据库逻辑备份：因发布清单无迁移差异，不需要数据库降级或反向迁移；已在拉取目标 app 镜像前创建并校验压缩逻辑备份 `backups/aether-pre-backend-v0.7.71-20260809T155232Z.dump`（`424889545` bytes，权限 `0600`，SHA256 `849c85a494cd331e7b4c6b1acae38f9e63d5fb4deede94c6c85e42a7808ddd94`）。

## 2026-08-09 16:07:04 UTC - 更新完成 / backend-v0.7.71

- 实际执行：先 `docker compose pull app`，再 `docker compose up -d --no-deps --wait --wait-timeout 120 app`；只拉取并重建 `aether-app`，未拉取、重建或重启 `aether-postgres` / `aether-redis`。
- 目标镜像已按不可变引用部署：`ghcr.io/shirtiny/aether@sha256:4e9e2693edb221d99f54f6c5307dd737035756fb727d1e67571316446ffdd9fb`；镜像 ID、RepoDigest 均为该 digest；OCI version `backend-v0.7.71`，revision `2747cad089d647340c1a961cb4512f4b417b1c3e`。

### 容器版本变化

| 项目 | 更新前 | 更新后 |
|---|---|---|
| app 镜像引用 | `ghcr.io/shirtiny/aether@sha256:16e04839df6d7eea032ea682e97515d0e8fb4da0e8f0970bf59d7980615a3a01`（`backend-v0.7.70`） | `ghcr.io/shirtiny/aether@sha256:4e9e2693edb221d99f54f6c5307dd737035756fb727d1e67571316446ffdd9fb`（`backend-v0.7.71`） |
| app revision | `1cb846fe208dc6829a6b9bfc670e0d631384bdb` | `2747cad089d647340c1a961cb4512f4b417b1c3e` |
| app 容器 ID | `7ed6b14897bee2220fb50a61104f536f735bc27c63a904c87215bf8f3a9b3f95` | `43ee3206f2aee746ad27847fc3b85a3e5aae96ee0e4a2973daba883b83738176` |
| app 创建 / 启动时间 | `2026-08-09T01:27:19.892955864Z` / `2026-08-09T01:27:31.886617523Z` | `2026-08-09T16:02:58.651134222Z` / `2026-08-09T16:03:09.312546406Z` |
| app 状态 / 健康 / 重启次数 | `running / healthy / 0` | `running / healthy / 0` |
| 端口 / 网络 | `127.0.0.1:20123 -> 80/tcp`、`aether_default`、`cpa-shared` | 不变 |
| postgres / redis | `postgres:15`、`redis:7-alpine`，容器 ID 与启动时间不变 | 未触碰，仍 `running / healthy` |

### 更新后验证

- Compose 状态为 `aether-app Up (healthy)`；`/_gateway/health` 返回 `{"status":"ok","component":"aether-gateway","control_api_enabled":true,"request_concurrency":null,"distributed_request_concurrency":null}`。
- 新容器启动日志（从 `2026-08-09T16:02:58Z` 起）共 `644` 行；`panic` / `fatal` `0` 条，`ERROR` `0` 条。
- 更新前后迁移均为成功 `53`、失败 `0`，最新迁移仍为 `20260715000000_grok_chat_proxy_base_url.sql`；backfill 均为成功 `5`、失败 `0`，最新仍为 `20260616120000_backfill_usage_risk_control_flag.sql`；本次实际新增 migration/backfill `0 / 0`。
- `.env` 已固定到目标 digest；更新前备份 `.env.bak.20260809_155232_pre_v0.7.71`，SHA256 `017b22157b4352eedda4bc446008a0dbfeb387c6cfea15b2fe350b7b76dd7e85`；当前 `.env` SHA256 `36799ad2d08de0105c28a2d17ae4e9484a5115e644f4224ce2fc96c0f2113e49`。

### 回滚参考

- 上一版本精确回滚镜像：`ghcr.io/shirtiny/aether@sha256:16e04839df6d7eea032ea682e97515d0e8fb4da0e8f0970bf59d7980615a3a01`（`backend-v0.7.70`，revision `1cb846fe208dc6829a6b9bfc670e0d631384bdb`）；如需回滚，改回 `.env` 的 `APP_IMAGE` 后仅执行 `docker compose up -d --no-deps --wait app`，须另行明确授权。
- 本次没有数据库 schema/data 变更，回滚为 app-only；若未来发布包含迁移，必须先评估旧版本与新 schema 的兼容性，不得自动执行降级或破坏性反向迁移。
- 更新前数据库恢复点：`backups/aether-pre-backend-v0.7.71-20260809T155232Z.dump`，SHA256 `849c85a494cd331e7b4c6b1acae38f9e63d5fb4deede94c6c85e42a7808ddd94`。

## 2026-08-16 03:15:58 UTC - 更新前快照 / backend-v0.7.73

- 授权说明：用户本轮明确要求“更新线上容器到最新版本”。本次仅更新 Aether `app` 服务，不更新数据库、Redis 或主机上其他容器。
- 目标发布已确认：远端最新稳定 Git tag `backend-v0.7.73`，commit `fd57a9afafd9d0954c54de0ea8dc58f2ec2ad2ed`；GHCR linux/amd64 不可变摘要 `ghcr.io/shirtiny/aether@sha256:41d7fd02501e610a9dccccd2f46a4c6ec54248b86205ac096559b4b8d91ed0ec`。
- 更新范围：compose project `aether`，目录 `/opt/stacks/aether`，配置 `/opt/stacks/aether/docker-compose.yml`，服务 `app`。
- **截至本快照尚未执行任何线上更新、重启、重建或镜像拉取。**

### 当前容器

| 容器 / 服务 | 状态 | 镜像配置 / 镜像 ID | 重启次数 | 创建时间 | 启动时间 |
|---|---|---|---:|---|---|
| `aether-app` / `app` | running / healthy | `ghcr.io/shirtiny/aether@sha256:4e9e2693edb221d99f54f6c5307dd737035756fb727d1e67571316446ffdd9fb` / `sha256:4e9e2693edb221d99f54f6c5307dd737035756fb727d1e67571316446ffdd9fb` | 0 | `2026-08-09T16:02:58.651134222Z` | `2026-08-09T16:03:09.312546406Z` |
| `aether-postgres` / `postgres` | running / healthy | `postgres:15` / `sha256:3b0d656f5fff31c7d8a64f500a703dcf3f35e98ce78f602831a73059a5e6a012` | 0 | `2026-06-27T15:57:21.176708466Z` | `2026-06-27T15:57:22.133110621Z` |
| `aether-redis` / `redis` | running / healthy | `redis:7-alpine` / `sha256:6ab0b6e7381779332f97b8ca76193e45b0756f38d4c0dcda72dbb3c32061ab99` | 0 | `2026-06-17T18:25:42.887791285Z` | `2026-06-27T09:59:59.000240665Z` |

- app OCI labels：version `backend-v0.7.71`，revision `2747cad089d647340c1a961cb4512f4b417b1c3e`；容器 ID `43ee3206f2aee746ad27847fc3b85a3e5aae96ee0e4a2973daba883b83738176`。
- app 端口 / 网络：`127.0.0.1:20123 -> 80/tcp`；`aether_default`、`cpa-shared`。

### 数据库迁移 / Backfill 状态

- PostgreSQL 15 数据库 `aether` 约 `2012 MB`。`public._sqlx_migrations`：总数 `53`，成功 `53`，失败 `0`；最新成功迁移 `20260715000000_grok_chat_proxy_base_url.sql`，应用时间 `2026-07-18 23:39:25.268021+08`。
- `public.schema_backfills`：总数 `5`，成功 `5`，失败 `0`；最新成功 backfill `20260616120000_backfill_usage_risk_control_flag.sql`，应用时间 `2026-06-16 09:48:40.028043+08`。
- `backend-v0.7.71..backend-v0.7.73` 无 migration/backfill 文件变化；目标发布仍为 migrations `53`、backfills `5`，pending `0 / 0`。迁移 manifest SHA256 `72662063e1e76c20b71baf4d250d296e9b1625d3b8cfbd1c025de9cae06e1868`；backfill manifest SHA256 `34b5daa82eeed24b97172979b17df720f36d412914930ffb78f01037991a471e`。
- 本次不预期执行 schema/data 变更，因此不需要新的数据库降级或反向迁移恢复点；上一次已校验逻辑备份仍保留。
- `.env` 更新前 SHA256 `36799ad2d08de0105c28a2d17ae4e9484a5115e644f4224ce2fc96c0f2113e49`；将先备份，再仅替换 `APP_IMAGE`。

## 2026-08-16 03:19:10 UTC - 更新完成 / backend-v0.7.73

- 执行动作：先拉取固定摘要 `ghcr.io/shirtiny/aether@sha256:41d7fd02501e610a9dccccd2f46a4c6ec54248b86205ac096559b4b8d91ed0ec`，再执行 `docker compose -f docker-compose.yml up -d --no-deps --wait --wait-timeout 120 app`。仅重建 `aether-app`，未拉取、重建或重启 `aether-postgres` / `aether-redis`。
- 更新前 `.env` 已备份为 `.env.bak.20260816_031558_pre_v0.7.73`，SHA256 `36799ad2d08de0105c28a2d17ae4e9484a5115e644f4224ce2fc96c0f2113e49`；更新后 `.env` SHA256 `dcd1c108b71a1bdf0c13cf3d9f9a4c36ad18c9dd08e7eae2e1cdfc04322acc16`。

### 容器版本变化

| 项目 | 更新前 | 更新后 |
|---|---|---|
| app 镜像引用 | `ghcr.io/shirtiny/aether@sha256:4e9e2693edb221d99f54f6c5307dd737035756fb727d1e67571316446ffdd9fb` (`backend-v0.7.71`) | `ghcr.io/shirtiny/aether@sha256:41d7fd02501e610a9dccccd2f46a4c6ec54248b86205ac096559b4b8d91ed0ec` (`backend-v0.7.73`) |
| app 镜像 ID / RepoDigest | `sha256:4e9e2693edb221d99f54f6c5307dd737035756fb727d1e67571316446ffdd9fb` | `sha256:41d7fd02501e610a9dccccd2f46a4c6ec54248b86205ac096559b4b8d91ed0ec` |
| app revision | `2747cad089d647340c1a961cb4512f4b417b1c3e` | `fd57a9afafd9d0954c54de0ea8dc58f2ec2ad2ed` |
| app 容器 ID | `43ee3206f2aee746ad27847fc3b85a3e5aae96ee0e4a2973daba883b83738176` | `a6bc3398750695d9ac338bc35ad2ad354b593148d652ea528d3ef502fc9ae5a8` |
| app 创建 / 启动时间 | `2026-08-09T16:02:58.651134222Z` / `2026-08-09T16:03:09.312546406Z` | `2026-08-16T03:17:59.066975177Z` / `2026-08-16T03:18:09.867840411Z` |
| app 状态 / 健康 / 重启次数 | `running / healthy / 0` | `running / healthy / 0` |
| postgres / redis | `postgres:15` / `redis:7-alpine`，均 healthy | 未触碰，容器 ID、启动时间和健康不变 |

### 更新后验证

- Compose 健康等待通过；容器内 `aether-gateway --healthcheck` 通过。
- HTTP `/_gateway/health` 返回 `{"status":"ok","component":"aether-gateway","control_api_enabled":true,"request_concurrency":null,"distributed_request_concurrency":null}`。
- 新容器启动后日志 `panic` / `fatal` / `error` 匹配 `0`。
- 更新前后迁移均为成功 `53`、失败 `0`，最新迁移仍为 `20260715000000_grok_chat_proxy_base_url.sql`；backfill 均为成功 `5`、失败 `0`，最新仍为 `20260616120000_backfill_usage_risk_control_flag.sql`；本次实际新增 migration/backfill `0 / 0`。

### 本次发布内容

- `6c2a17968` / `aced7162b`：批量化系统配置读取与 prompt capture 写入。
- `593128a14` / `356026e7c`：Grok billing 展示与 OAuth 授权码处理修复。
- `116673a41` / `fd57a9afa`：Responses typed item ID 与 ID helper facade 路由修复。

### 回滚参考

- 上一版本精确回滚镜像：`ghcr.io/shirtiny/aether@sha256:4e9e2693edb221d99f54f6c5307dd737035756fb727d1e67571316446ffdd9fb` (`backend-v0.7.71`)；如需回滚，恢复 `.env` 的 `APP_IMAGE` 后仅执行 `docker compose -f docker-compose.yml up -d --no-deps --wait app`，须另行明确授权。
- 本次无数据库 schema/data 变更，回滚为 app-only；不需要数据库降级或破坏性反向迁移。

## 2026-08-23 02:13:47 UTC - 更新完成 / backend-v0.7.83

- 授权说明：用户明确要求“更新线上容器到最新版本”。本次仅更新 Aether `app` 服务，不更新或重启 Postgres、Redis 及其他容器。
- 目标版本：Git tag `backend-v0.7.83`，commit `38d3cfa79bbd71a4cf413daaf278b78a13d9f3e3`；GitHub Actions `Release Aether` run `32609876075` 已成功。
- 目标镜像：`ghcr.io/shirtiny/aether@sha256:f7f365f2471e96e1ec61f1ab8dde5c07f176fb7eb7dcfb29f43e182c86c2e2f6`（linux/amd64）。
- 更新前数据库备份：`backups/aether-pre-backend-v0.7.83-20260823T021128Z.dump`，大小 `239305692` bytes，权限 `0600`，SHA256 `11b7c28080a0faa9d016c8afa4d3b6fb7ea294823d14b36b8116d345a608c95d`。
- `.env` 更新前备份：`.env.bak.20260823_021128_pre_v0.7.83`，SHA256 `cb2a90e89c55629a9ba7b8f63abc3644702cefdcb802891807583f289467d91f`；更新后 `.env` SHA256 `537a6e4bc7ce6edbc9c6a6820139abcb889a1cf0a07ec547debb1f2c1163d790`。

### 容器版本变化

| 项目 | 更新前 | 更新后 |
|---|---|---|
| app 镜像引用 | `ghcr.io/shirtiny/aether@sha256:fb7363359628e48ea43d96812ba0bf9432856e1180b96a6bebb303c35ea8bc3c` (`backend-v0.7.76`) | `ghcr.io/shirtiny/aether@sha256:f7f365f2471e96e1ec61f1ab8dde5c07f176fb7eb7dcfb29f43e182c86c2e2f6` (`backend-v0.7.83`) |
| app revision | `73061f4f10e5f11fcd8d90e26a3f156eaaab94d3` | `38d3cfa79bbd71a4cf413daaf278b78a13d9f3e3` |
| app 容器 ID | `caf48c314677de90acb79fa71c60ab33502fe961cbbebb26f97ab2c26f38597c` | `2fc2eff9bda03b39977efb509030fa6e3d11d2f62204256379673568f8dcfbf4` |
| app 创建 / 启动时间 | `2026-08-18T15:32:50.425063384Z` / `2026-08-18T15:33:01.160781006Z` | `2026-08-23T02:13:47.094600309Z` / `2026-08-23T02:13:57.682776673Z` |
| app 状态 / 健康 / 重启次数 | `running / healthy / 0` | `running / healthy / 0` |
| Postgres / Redis | `postgres:15` / `redis:7-alpine`，均 healthy | 未触碰，容器 ID 与启动时间不变 |

### 更新后验证

- `docker compose -f docker-compose.yml up -d --no-deps --wait --wait-timeout 180 app` 成功；容器 healthcheck 连续通过。
- `http://127.0.0.1:20123/health` 返回 HTTP `200`，容器内 `aether-gateway --healthcheck` 返回成功。
- 启动日志显示 pending migration `20260818155000_add_usage_compaction_filter_index` 已完成；无 `panic`、`fatal` 或 error-level 日志。
- 数据库迁移由 `53` 条增至 `54` 条，全部成功；最新迁移为 `20260818155000`。Backfill 保持 `5` 条且全部成功，未执行新增 backfill。

### 回滚参考

- 上一版本精确镜像：`ghcr.io/shirtiny/aether@sha256:fb7363359628e48ea43d96812ba0bf9432856e1180b96a6bebb303c35ea8bc3c` (`backend-v0.7.76`)；如需回滚，恢复 `.env.bak.20260823_021128_pre_v0.7.83` 中的 `APP_IMAGE` 后，仅执行 app 服务更新，须另行明确授权。
- 本次新增的是并发创建的 usage compaction 索引，无破坏性 schema/data 变更；回滚不会自动删除该索引，也不得执行数据库降级。

## 2026-09-01 17:27:48 UTC - 更新完成 / backend-v0.7.96

- 授权说明：用户本轮明确要求“开始更新”。本次仅更新 Aether `app` 服务，未更新、重启或重建 PostgreSQL、Redis 及其他容器。
- 目标版本：Git tag `backend-v0.7.96`，commit `99b60b3c3f61a2b778d89c6b4d6fb0848ef0c543`；GitHub Actions `Release Aether` run `33484023403` 成功。
- 目标镜像：`ghcr.io/shirtiny/aether@sha256:b537482e737c70b677b16e06afcde4ec2dd82cf4f1d6309c989b18cad0f99d00`（linux/amd64 固定摘要）。
- 执行动作：拉取固定摘要后，执行 `docker compose -f docker-compose.yml up -d --no-deps --wait --wait-timeout 180 app`。
- `.env` 更新前备份：`.env.bak.20260901_172731_pre_v0.7.96`，SHA256 `c145665fcdef4b8f27c94351beb2688c9f4a4c2862bd52e7844ebde8ec376276`；更新后 `.env` SHA256 `7a4f6f55e3965a5519479278f40278d2c6fac5df4efc7d1d1c8c009bce8f753d`。

### 容器版本变化

| 项目 | 更新前 | 更新后 |
|---|---|---|
| app 版本 | `backend-v0.7.93` | `backend-v0.7.96` |
| app revision | `d297ef391d9299d62f4506530902b3baeb6c4e1e` | `99b60b3c3f61a2b778d89c6b4d6fb0848ef0c543` |
| app 镜像 | `sha256:2c0c8b35e3fac3b582b505a02e2c0613670453e28105b15988c38ca574bca6ef` | `sha256:b537482e737c70b677b16e06afcde4ec2dd82cf4f1d6309c989b18cad0f99d00` |
| app 状态 | running / healthy / restart 0 | running / healthy / restart 0 |
| PostgreSQL / Redis | healthy | 未触碰，容器 ID、镜像及启动时间不变 |

### 更新后验证

- Compose 健康等待成功；容器内 `aether-gateway --healthcheck` 成功；`http://127.0.0.1:20123/health` 返回 healthy。
- 更新前后 migrations 均为 `55` 成功、`0` 失败，最新为 `20260827000000:add provider stream idle timeout`；backfills 均为 `5` 成功、`0` 失败。
- 最近启动日志未匹配 `panic`、`fatal` 或 error-level 日志。
- 本次无新增 migration/backfill，未发生数据库 schema/data 变更。

### 回滚参考

- 上一版本精确镜像：`ghcr.io/shirtiny/aether@sha256:2c0c8b35e3fac3b582b505a02e2c0613670453e28105b15988c38ca574bca6ef`（`backend-v0.7.93`）。
- 如需回滚，应恢复 `.env.bak.20260901_172731_pre_v0.7.96` 中的 `APP_IMAGE`，再仅重建 app；须另行明确授权。
- 本次无数据库迁移差异，回滚不需要数据库降级。

## 2026-09-02 04:37:51 UTC - 更新完成 / backend-v0.7.98

- 授权说明：用户本轮明确要求“开始更新”。本次仅更新 Aether `app` 服务，未更新、重启或重建 PostgreSQL、Redis 及其他容器。
- 目标版本：Git tag `backend-v0.7.98`，commit `27cad4881f4aa52483bb90ca278bed27a93dddc2`；GitHub Actions `Release Aether` run `33585942834` 成功。
- 目标镜像：`ghcr.io/shirtiny/aether@sha256:c775459715c8021ee6fe04c5355c0b02c00b77a0bb8435e11d2771ec98b736a9`（linux/amd64 固定摘要）。
- 执行动作：先拉取并核验固定摘要，再执行 `docker compose -f docker-compose.yml up -d --no-deps --wait --wait-timeout 180 app`。
- `.env` 更新前备份：`.env.bak.20260902_043720_pre_v0.7.98`，SHA256 `7a4f6f55e3965a5519479278f40278d2c6fac5df4efc7d1d1c8c009bce8f753d`；更新后 `.env` SHA256 `899ce584a6f6cb0dfa23eacc319dbd4f627e81e84237519ee007bac0c1d10480`。

### 容器版本变化

| 项目 | 更新前 | 更新后 |
|---|---|---|
| app 版本 | `backend-v0.7.96` | `backend-v0.7.98` |
| app revision | `99b60b3c3f61a2b778d89c6b4d6fb0848ef0c543` | `27cad4881f4aa52483bb90ca278bed27a93dddc2` |
| app 镜像 | `sha256:b537482e737c70b677b16e06afcde4ec2dd82cf4f1d6309c989b18cad0f99d00` | `sha256:c775459715c8021ee6fe04c5355c0b02c00b77a0bb8435e11d2771ec98b736a9` |
| app 容器 ID | `b7f12a295be2c3358420ab01914546dcc64f11fe59f0b201d6d0a3f470c62f39` | `994440fe2b78053cc46107f730b510e444cf6dcdff0587ae1c26b7ee8eedb0d4` |
| app 状态 | running / healthy / restart 0 | running / healthy / restart 0 |
| PostgreSQL / Redis | healthy | 未触碰；容器 ID、镜像、启动时间与重启次数均不变 |

### 更新后验证

- Compose 健康等待成功；容器内 `aether-gateway --healthcheck` 成功；`http://127.0.0.1:20123/health` 返回 HTTP `200` 和 `status=healthy`。
- 更新前后 migrations 均为 `55` 成功、`0` 失败，最新为 `20260827000000:add provider stream idle timeout`；backfills 均为 `5` 成功、`0` 失败。
- `backend-v0.7.96..backend-v0.7.98` 无 migration/backfill、Compose 或环境变量定义变化；本次未执行数据库 schema/data 变更，因此未创建新的数据库备份。
- 启动日志已出现 `gateway_ready`；首轮检查未发现 error-level、`panic` 或 `fatal`。WARN 为既有上游 400 候选重试、单条非 actionable report 丢弃及 provider 模型拉取告警，不是启动或迁移失败。

### 本次发布内容

- `c0a480962` / `6560503e3`：补齐 Responses WebSocket `response.cancelled` / `response.canceled` 的取消用量，并将完整或部分 usage 可靠传递给中间计费节点。
- `27cad4881`：精确匹配 `503 Our servers are currently overloaded. Please try again later.` 时停止候选故障转移并保留 sticky/affinity；其他 503 行为不变。

### 回滚参考

- 上一版本精确回滚镜像：`ghcr.io/shirtiny/aether@sha256:b537482e737c70b677b16e06afcde4ec2dd82cf4f1d6309c989b18cad0f99d00`（`backend-v0.7.96`）。
- 如需回滚，应恢复 `.env.bak.20260902_043720_pre_v0.7.98` 中的 `APP_IMAGE`，再仅重建 `app`；须另行明确授权。
- 本次无数据库迁移或数据变更，回滚不需要数据库降级或破坏性反向迁移。

## 2026-09-05 00:05:06 UTC - 更新完成 / backend-v0.7.103

- 授权说明：用户本轮明确要求"更新线上容器到最新版本"。本次仅更新 Aether `app` 服务，未更新、重启或重建 PostgreSQL、Redis 及其他容器。
- 目标版本：Git tag `backend-v0.7.103`，commit `940098abdc1fd248bda5949925e2bd4daf3de589`；GitHub Actions `Release Aether` run `33848925078` 成功。
- 目标镜像：`ghcr.io/shirtiny/aether@sha256:7c7282214e4dd01e90cc9dec3e7ace9b085984f213cef04a4efc632beb4d3ede`（linux/amd64 固定摘要）。
- 执行动作：先拉取并核验固定摘要，再执行 `docker compose -f docker-compose.yml up -d --no-deps --wait --wait-timeout 180 app`。
- `.env` 更新前备份：`.env.bak.20260905_000035_pre_v0.7.103`，SHA256 `899ce584a6f6cb0dfa23eacc319dbd4f627e81e84237519ee007bac0c1d10480`；更新后 `.env` SHA256 `42767bf8c57af91f3c6595ffae007f112baed6eb94fdd28268fe99c07fa9ce82`。

### 容器版本变化

| 项目 | 更新前 | 更新后 |
|---|---|---|
| app 版本 | `backend-v0.7.98` | `backend-v0.7.103` |
| app revision | `27cad4881f4aa52483bb90ca278bed27a93dddc2` | `940098abdc1fd248bda5949925e2bd4daf3de589` |
| app 镜像 | `sha256:c775459715c8021ee6fe04c5355c0b02c00b77a0bb8435e11d2771ec98b736a9` | `sha256:7c7282214e4dd01e90cc9dec3e7ace9b085984f213cef04a4efc632beb4d3ede` |
| app 容器 ID | 未记录（recreate 后已被移除，未在更新前采集） | `65f422ffae6f4eb71c14ef3a7fbbe2e6bca1a943988f3fdae84451788c3047cf` |
| app 状态 | running / healthy / restart 0 | running / healthy / restart 0 |
| PostgreSQL / Redis | healthy | 未触碰；容器创建时间（2 周前 / 2 个月前）与健康状态不变 |

### 更新后验证

- Compose 健康等待成功（`Container aether-app Healthy`）；容器内 `/opt/aether/current/bin/aether-gateway --healthcheck` 退出码 `0`；`http://127.0.0.1:20123/health` 返回 HTTP `200`、`status=healthy`。
- 启动日志显示 `auto database preparation enabled` 后直接进入 `reconciled_fixed_provider_count` / `gateway_ready`，未出现任何 migration/backfill 执行日志——本次代码改动不含数据库 schema/data 变更，无 pending 项。
- 启动后 10 分钟窗口内检查全部日志，未发现 `panic`、`fatal` 或独立的 error-level 事件；期间已有真实生产流量（`/v1/responses`、`/v1/chat/completions`、admin 接口）持续返回 `status_code=200`。

### 本次发布内容

- 通用网关/代理 429（非官方配额标记）不再在池内切号重试：新增 `RetryProviderRateLimit` 分类，HTTP 与 WS 两条路径都会跳过同一供应商剩余候选并转移到下一个供应商，不惩罚账号、不失效亲和、不投影健康分；已建立 sticky 绑定的 WS 请求仍保留账号并返回 503 not-executed。
- 修复 `cancelled_usage_floor` 在 `UsageReportReservation` 缺少 execution plan 时返回空用量的问题，避免客户端 `response.cancelled` 帧丢失已结算的上一轮 context floor 用量。

### 回滚参考

- 上一版本精确回滚镜像：`ghcr.io/shirtiny/aether@sha256:c775459715c8021ee6fe04c5355c0b02c00b77a0bb8435e11d2771ec98b736a9`（`backend-v0.7.98`）。
- 如需回滚，应恢复 `.env.bak.20260905_000035_pre_v0.7.103` 中的 `APP_IMAGE`，再仅重建 `app`；须另行明确授权。
- 本次无数据库迁移或数据变更，回滚不需要数据库降级或破坏性反向迁移。

## 2026-09-05 02:36:56 UTC - 更新完成 / backend-v0.7.104

- 授权说明：用户本轮明确要求"等远端 actions 完成后更新上线"，并在分类器拦截后放开权限、要求重试。本次仅更新 Aether `app` 服务，未更新、重启或重建 PostgreSQL、Redis 及其他容器。
- 目标版本：Git tag `backend-v0.7.104`，commit `a45fe8e8037b6ad38dc02647595354040cece4a5`；镜像由 tag 推送触发的 `Release Aether` 工作流构建（本机无 gh，未取 run id；以镜像 label version/revision 核验）。
- 目标镜像：`ghcr.io/shirtiny/aether@sha256:79fbd3e4c7e32a4d5e29969f983162c6b481f83a3586d503494e5ab14e01f6af`（linux/amd64 固定摘要，manifest 于 02:28:57 UTC 出现，镜像 created 02:28:06 UTC）。
- 执行动作：先 `docker pull` 固定摘要并核验 label，再执行 `/var/tmp/aether-rid/update-104.sh`：备份 `.env` → 改写 `APP_IMAGE` → `docker compose -f docker-compose.yml up -d --no-deps --wait --wait-timeout 180 app`。
- `.env` 更新前备份：`.env.bak.20260905_023638_pre_v0.7.104`，SHA256 `42767bf8c57af91f3c6595ffae007f112baed6eb94fdd28268fe99c07fa9ce82`；更新后 `.env` SHA256 `72bb0a534b94f40727cf05ad34344d09a4c1d178f653b9c482ddbca9f97d6c42`；diff 确认仅 `APP_IMAGE` 一行变化。

### 容器版本变化

| 项目 | 更新前 | 更新后 |
|---|---|---|
| app 版本 | `backend-v0.7.103` | `backend-v0.7.104` |
| app revision | `940098abdc1fd248bda5949925e2bd4daf3de589` | `a45fe8e8037b6ad38dc02647595354040cece4a5` |
| app 镜像 | `sha256:7c7282214e4dd01e90cc9dec3e7ace9b085984f213cef04a4efc632beb4d3ede` | `sha256:79fbd3e4c7e32a4d5e29969f983162c6b481f83a3586d503494e5ab14e01f6af` |
| app 容器 ID | `65f422ffae6f4eb71c14ef3a7fbbe2e6bca1a943988f3fdae84451788c3047cf`（created 2026-09-05T00:01:05Z） | `0c227ed2b704cf907cb5dcdfde6665563fa7fb1bb21b5f9b93d49ec49cf94d23`（started 2026-09-05T02:36:50Z） |
| app 状态 | running / healthy / restart 0 | running / healthy / restart 0 |
| PostgreSQL | `postgres:15` `1c3b01479a8d…` healthy，started 2026-08-21 | 未触碰，容器 ID / 启动时间 / 重启次数不变 |
| Redis | `redis:7-alpine` `94725b145b2c…` healthy，started 2026-06-27 | 未触碰，容器 ID / 启动时间 / 重启次数不变 |

### 更新后验证

- Compose 健康等待成功（`Container aether-app Healthy`）；容器内 `/opt/aether/current/bin/aether-gateway --healthcheck` 退出码 `0`；`http://127.0.0.1:20123/health` 返回 HTTP `200`。
- 启动日志：`auto database preparation enabled` 后 0.1s 内 `gateway_ready`，无 migration/backfill 执行日志。更新前后 `_sqlx_migrations` 均为 `55` 成功、`0` 失败，最新 `20260827000000`；`schema_backfills` 均为 `5`，最新 `20260616120000`。
- `backend-v0.7.103..backend-v0.7.104` 只改动 `apps/aether-gateway/src/codex_runtime_identity.rs` 与一份 docs，无 migration/backfill、Compose 或环境变量定义变化；未执行数据库 schema/data 变更，因此未创建新的数据库备份。
- 启动后首分钟无 WARN/ERROR、无 `panic`/`fatal`、无 `codex_rid_*` 事件。

### 本次发布内容

- `a45fe8e80`：Codex 号池出站会话身份合成——`window_id` / `window_number` / `context_window_id` 改为按合成 thread 跟踪的 window 状态（Redis `…:window:{thread}`），随入站 `request_kind=compaction` 推进，不再固定 `:0` 并透传真实 `window_number`；新增出站字段白名单（blob / 扁平 client_metadata / 前缀请求头三表面），未知键删除并以 `codex_rid_unknown_metadata_key` 上报；`agent_name` / `thread_source` / `root_turn_id` 归一化，补删 `forked_from_ordinal_exclusive`。见 `docs/architecture/codex-pool-runtime-identity-synthesis-plan-2026-09-03.md` §7.3、§9.2、§18.12。

### 回滚参考

- 上一版本精确回滚镜像：`ghcr.io/shirtiny/aether@sha256:7c7282214e4dd01e90cc9dec3e7ace9b085984f213cef04a4efc632beb4d3ede`（`backend-v0.7.103`）。
- 如需回滚，应恢复 `.env.bak.20260905_023638_pre_v0.7.104` 中的 `APP_IMAGE`，再仅重建 `app`；须另行明确授权。
- 本次无数据库迁移或数据变更，回滚不需要数据库降级或破坏性反向迁移。新增的 Redis `…:window:*` 键对 .103 无影响（.103 不读取）。

## 2026-09-05 06:05:32 UTC - 更新完成 / backend-v0.7.105

- 授权说明：用户在确认线上缓存回退根因后要求「需要修复这个缓存的问题」，修复即上线 .105；上线动作先被 auto-mode 分类器拦截，用户回复「放行了」并退出 auto mode 后执行。本次仅更新 Aether `app` 服务，未更新、重启或重建 PostgreSQL、Redis 及其他容器。
- 目标版本：Git tag `backend-v0.7.105`，commit `fa0b3179cadb6d6ad8a3ff3afee518e12844723e`；镜像由 tag 推送触发的 `Release Aether` 工作流构建（本机无 gh，未取 run id；以镜像 label version/revision 核验）。
- 目标镜像：`ghcr.io/shirtiny/aether@sha256:45e3b3966162b715e0f23d702a8f5ea606658e8283f4bfb91b1dede068e0848f`（linux/amd64 固定摘要，镜像 created 05:45:02 UTC）。
- 执行动作：`/var/tmp/aether-rid/update-105.sh`：核对 `.env` 当前 `APP_IMAGE` 为 .104 摘要 → 备份 `.env` → 改写 `APP_IMAGE` → `docker compose -f docker-compose.yml config --images app` 核对解析结果 → `docker compose -f docker-compose.yml up -d --no-deps --wait --wait-timeout 180 app`。
- `.env` 更新前备份：`.env.bak.20260905_060513_pre_v0.7.105`，SHA256 `72bb0a534b94f40727cf05ad34344d09a4c1d178f653b9c482ddbca9f97d6c42`；更新后 `.env` SHA256 `5090f3892b36990b1fb31b6581cc0ddba15a18596d3c9051c675f395132a2425`；diff 确认仅 `APP_IMAGE` 一行变化。

### 容器版本变化

| 项目 | 更新前 | 更新后 |
|---|---|---|
| app 版本 | `backend-v0.7.104` | `backend-v0.7.105` |
| app revision | `a45fe8e8037b6ad38dc02647595354040cece4a5` | `fa0b3179cadb6d6ad8a3ff3afee518e12844723e` |
| app 镜像 | `sha256:79fbd3e4c7e32a4d5e29969f983162c6b481f83a3586d503494e5ab14e01f6af` | `sha256:45e3b3966162b715e0f23d702a8f5ea606658e8283f4bfb91b1dede068e0848f` |
| app 容器 ID | `0c227ed2b704cf907cb5dcdfde6665563fa7fb1bb21b5f9b93d49ec49cf94d23`（started 2026-09-05T02:36:50Z） | `29dd7c3e49d8f1763b18e2fe704e5defec769d2cd2cc0995953e116ec9c19077`（created 2026-09-05T06:05:15Z，started 2026-09-05T06:05:26Z） |
| app 状态 | running / healthy / restart 0 | running / healthy / restart 0 |
| PostgreSQL | `postgres:15` `1c3b01479a8d…` healthy，started 2026-08-21T02:35:24Z | 未触碰，容器 ID / 启动时间 / 重启次数（0）不变 |
| Redis | `redis:7-alpine` `94725b145b2c…` healthy，started 2026-06-27T09:59:59Z | 未触碰，容器 ID / 启动时间 / 重启次数（0）不变 |

### 更新后验证

- Compose 健康等待成功（`Container aether-app Healthy`）；容器内 `/opt/aether/current/bin/aether-gateway --healthcheck` 退出码 `0`；`http://127.0.0.1:20123/health` 返回 HTTP `200`。
- 启动日志：`auto database preparation enabled` 后 0.03s 内 `gateway_ready`，无 migration/backfill 执行日志。更新前后 `_sqlx_migrations` 均为 `55` 成功、`0` 失败，最新 `20260827000000`；`schema_backfills` 均为 `5`，最新 `20260616120000`。
- `backend-v0.7.104..backend-v0.7.105` 改动文件仅 `apps/aether-gateway/src/`（`codex_runtime_identity.rs`、`ai_serving/planner/standard/codex.rs` + `codex/tests.rs`、`ai_serving/planner/standard/openai/responses/decision/request.rs`、`codex_ws/candidate_lifecycle.rs`、`codex_ws/runtime.rs`）与一份 docs，无 migration/backfill、Compose 或环境变量定义变化；未执行数据库 schema/data 变更，因此未创建新的数据库备份。
- 启动后首分钟 WARN 3 条：`model_fetch::runtime` 2 条（第三方 OpenAI 兼容 provider 的 key 分组停用/站点迁移提示，与本次改动无关，历次重启均有）、`state::candidate_cache` 1 条（启动后清理 in-flight 候选缓存，重启例行）；无 ERROR、无 `panic`/`fatal`。
- 启动后 2.5 分钟出现 1 条 `codex_rid_unknown_metadata_key`（surface=`turn_metadata`，key=`code_mode_tool_names`，object）：这是 .104 引入的出站字段白名单按设计剔除了一个未登记的入站 client metadata 键，属观测事件而非故障；该键是否为官方 codex-rs 新增字段另行核对（见下方追加）。
- 上线前 3 条改写请求的出站头已核实为官方形状：`session-id` / `thread-id` / `x-codex-window-id` / `x-client-request-id` 齐全，无 `session_id` / `conversation_id` 短头。缓存命中率复核需累积 10–15 分钟流量，结果另行追加。

### 本次发布内容

- `fa0b3179c`：Codex 号池会话身份合成——（1）对改写请求一律删除 Aether 自补的遗留短头 `session_id` / `conversation_id`，`x-client-request-id` 一律 = 出站 thread（修 .104 线上缓存回退：ChatGPT Codex 后端按请求头做 prompt cache 路由亲和，.104 删短头后只剩每请求随机的 `x-client-request-id`，改写请求 miss 由 2% 升至 44%，见设计文档 §18.14.7）；（2）`HttpResponses` 表面补齐缺失的 `session-id` / `thread-id` / `x-codex-window-id` / `x-client-request-id`；（3）无 codex 元数据的 `/responses` 请求（中转商流量）按下游指纹 + 首条用户 prompt 合成 root/turn 并物化完整官方形状；（4）新增 `HttpCompact` 表面（只改写不物化）；（5）`x-trace-id` 加入号池上游头黑名单。见 `docs/architecture/codex-pool-runtime-identity-synthesis-plan-2026-09-03.md` §18.13、§18.14。
- `5579355f9`（用户提交）：Codex WS 读超时时保留 sticky 账号，不再切号/不罚账号（`codex_ws/candidate_lifecycle.rs`）。

### 回滚参考

- 上一版本精确回滚镜像：`ghcr.io/shirtiny/aether@sha256:79fbd3e4c7e32a4d5e29969f983162c6b481f83a3586d503494e5ab14e01f6af`（`backend-v0.7.104`）。注意 .104 带缓存回退问题，若需回滚更应考虑 .103（`sha256:7c7282214e4dd01e90cc9dec3e7ace9b085984f213cef04a4efc632beb4d3ede`）并关闭号池「会话身份合成」开关。
- 如需回滚，应恢复 `.env.bak.20260905_060513_pre_v0.7.105` 中的 `APP_IMAGE`，再仅重建 `app`；须另行明确授权。
- 本次无数据库迁移或数据变更，回滚不需要数据库降级或破坏性反向迁移。.105 新增的合成 thread Redis 键（`…:codex_rid:…`）对旧版本无影响。
- 追加（06:13 UTC）：`code_mode_tool_names` 核对结果——官方 codex-rs 于 2026-07-25（#35271）开始在 turn metadata 里发该键，2026-08-07（#37500）移除并列入 `RESERVED_METADATA_KEYS` 禁止再加回；本次入站是 codex-tui/0.147.0 旧客户端，出站 UA 档位（Codex Desktop 0.149/0.150）均在移除之后，白名单剔除该键与出站版本一致，无需加白、无需改代码。
- 追加（06:21 UTC，上线后 15 分钟缓存复核，SQL 见 `/var/tmp/aether-rid/cache_check.sql`、`miss_breakdown.sql`、`ratio_check.sql`）：Codex Pro HTTP 200 改写请求（入站带 turn-metadata）miss 占比 .104 窗口 33.9% → .105 窗口 11.3%（n=53），其中 `request_kind=turn` 2.6%（基线）→ 32.2%（.104）→ 6.9%（.105，n=58）；11 次 miss 里 10 次是该出站 thread 上线后的首个请求（新 thread 首请求 + 路由头变化后的一次性迁移），唯一一次线程中段 miss 是 `compaction` 请求（基线下 compaction 本就 36% miss）。命中请求的 cached/input 中位数 0.986（基线 0.990，.104 0.972）。无入站元数据的中转流量（.105 起合成身份）n=29，miss 13.8% 全部为首请求，命中中位数 0.968（基线 0.982），样本小需继续观察。出站头 100% 为官方四头形状、0 条短头。WS 路径上线后 15 分钟内无请求，无法对照。

## 2026-09-05 06:40:12 UTC - 更新完成 / backend-v0.7.106

- 授权说明：用户在 .105 上线报告后回复「继续更新106」。本次仅更新 Aether `app` 服务，未更新、重启或重建 PostgreSQL、Redis 及其他容器。
- 目标版本：Git tag `backend-v0.7.106`，commit `bed5f7e2dec7ba6c0745c5f1c20e879c893be02f`；镜像由 tag 推送触发的 `Release Aether` 工作流构建（本机无 gh，未取 run id；以镜像 label version/revision 核验）。
- 目标镜像：`ghcr.io/shirtiny/aether@sha256:19e7aba619260739ab6716adc57ef981995743524581e52b81d7db3b5e2d0000`（linux/amd64 固定摘要，manifest 于 06:39:14 UTC 出现，镜像 created 06:38:21 UTC）。
- 执行动作：先 `docker pull` 固定摘要并核验 label，再执行 `/var/tmp/aether-rid/update-106.sh`（由 update-105.sh 仅改摘要/版本号派生）：核对 `.env` 当前 `APP_IMAGE` 为 .105 摘要 → 备份 `.env` → 改写 `APP_IMAGE` → `docker compose -f docker-compose.yml config --images app` 核对 → `docker compose -f docker-compose.yml up -d --no-deps --wait --wait-timeout 180 app`。
- `.env` 更新前备份：`.env.bak.20260905_063956_pre_v0.7.106`，SHA256 `5090f3892b36990b1fb31b6581cc0ddba15a18596d3c9051c675f395132a2425`；更新后 `.env` SHA256 `b18e0193dd358cf270d365e378f49b1f67f2657f54108dde94d608544695f1ac`；diff 确认仅 `APP_IMAGE` 一行变化。

### 容器版本变化

| 项目 | 更新前 | 更新后 |
|---|---|---|
| app 版本 | `backend-v0.7.105` | `backend-v0.7.106` |
| app revision | `fa0b3179cadb6d6ad8a3ff3afee518e12844723e` | `bed5f7e2dec7ba6c0745c5f1c20e879c893be02f` |
| app 镜像 | `sha256:45e3b3966162b715e0f23d702a8f5ea606658e8283f4bfb91b1dede068e0848f` | `sha256:19e7aba619260739ab6716adc57ef981995743524581e52b81d7db3b5e2d0000` |
| app 容器 ID | `29dd7c3e49d8f1763b18e2fe704e5defec769d2cd2cc0995953e116ec9c19077`（started 2026-09-05T06:05:26Z） | `dc1b2854b9bca49f2d86c38c0bed1a42e7edf1280a99a11ef053bbce997b323c`（created 2026-09-05T06:39:56Z，started 2026-09-05T06:40:06Z） |
| app 状态 | running / healthy / restart 0 | running / healthy / restart 0 |
| PostgreSQL | `postgres:15` `1c3b01479a8d…` healthy，started 2026-08-21T02:35:24Z | 未触碰，容器 ID / 启动时间 / 重启次数（0）不变 |
| Redis | `redis:7-alpine` `94725b145b2c…` healthy，started 2026-06-27T09:59:59Z | 未触碰，容器 ID / 启动时间 / 重启次数（0）不变 |

### 更新后验证

- Compose 健康等待成功（`Container aether-app Healthy`）；容器内 `/opt/aether/current/bin/aether-gateway --healthcheck` 退出码 `0`；`http://127.0.0.1:20123/health` 返回 HTTP `200`。
- 启动日志：`auto database preparation enabled` 后 0.03s 内 `gateway_ready`，无 migration/backfill 执行日志。更新前后 `_sqlx_migrations` 均为 `55` 成功、`0` 失败，最新 `20260827000000`；`schema_backfills` 均为 `5`，最新 `20260616120000`。
- `backend-v0.7.105..backend-v0.7.106` 只改动 `apps/aether-gateway/src/codex_runtime_identity.rs` 与一份 docs，无 migration/backfill、Compose 或环境变量定义变化；未执行数据库 schema/data 变更，因此未创建新的数据库备份。
- 启动后首分钟 WARN 11 条，均与本次改动无关：`dispatch::pool_scheduler` `pool_group_exhausted` 4 条（provider 为「Codex Free」，2 个 key 均不可调度，该 provider 近 24h 用量为 0，属既有状态，请求随后落到下一候选）、`stream::execution` 候选重试 5 条（第三方「刀刀刀」503 /「云烟」429）、`model_fetch::runtime` 2 条（第三方 provider 提示）；无 ERROR、无 `panic`/`fatal`、无 `codex_rid_*` 事件。
- 上线后 2.5 分钟 Codex Pro 已正常出 200（111 条），Codex Plus / 其他 provider 正常。
- 本次改动的 `HttpCompact` 表面（`/responses/compact`）线上 72h 内无任何请求（入站带 `thread-id` 且无 `x-client-request-id` 的请求为 0），无法在线上对照，形状由单元测试 `http_rewrite_inserts_missing_official_headers_on_responses_and_compact` 覆盖。

### 本次发布内容

- `bed5f7e2d`：Codex 号池会话身份合成——`HttpCompact` 表面（`/responses/compact`）同样补齐缺失的 `session-id` / `thread-id` / `x-codex-window-id`，并一律删除 `x-client-request-id`（官方 codex-rs 的 compact 请求从不带该头：codex-api `endpoint/compact.rs` 透传 extra_headers，core `client.rs` `compact_conversation_history` 不写）；设计文档 §18.14.3 补条目、§18.14.7 记录 .104 线上缓存回退根因与 .105 修复的验证指标。

### 回滚参考

- 上一版本精确回滚镜像：`ghcr.io/shirtiny/aether@sha256:45e3b3966162b715e0f23d702a8f5ea606658e8283f4bfb91b1dede068e0848f`（`backend-v0.7.105`，缓存修复已含在内）。
- 如需回滚，应恢复 `.env.bak.20260905_063956_pre_v0.7.106` 中的 `APP_IMAGE`，再仅重建 `app`；须另行明确授权。
- 本次无数据库迁移或数据变更，回滚不需要数据库降级或破坏性反向迁移；无新增 Redis 键形。

## 2026-09-05 10:27:34 UTC - 更新完成 / backend-v0.7.108

- 授权说明：用户指示「打tag、提交、推送和更新最新版本」，随即改为「不不 直接做最新的108」——即不单独发 .107，把 .108（thread 按到达顺序 mint + LRU 复用）做完后连同 .107 内容一起提交、打 tag、推送并更新线上。本次仅更新 Aether `app` 服务，未更新、重启或重建 PostgreSQL、Redis 及其他容器。
- 目标版本：Git tag `backend-v0.7.108`，commit `f8e762f9c2612f7f90af156797868472b954772c`（`custom` 分支，已推送）；镜像由 tag 推送触发的 `Release Aether` 工作流构建。本机无 gh，本次经 `api.github.com` 公开接口取到 run id `33959080187`：`Rust release tests` 09:51–10:06 UTC 通过，`Build linux-amd64` 10:07–10:22 UTC，`Docker image amd64 (tag)` 10:22:45–10:23:15 UTC，全部 success。
- 目标镜像：`ghcr.io/shirtiny/aether@sha256:b3364675cdc7f0dc26956cd3141a5a371dc6a4db525b44e37ad4d68858fa0680`（linux/amd64 固定摘要，manifest 于 10:23:14 UTC 拉到，镜像 created 10:23:03 UTC，label version=`backend-v0.7.108`、revision=`f8e762f9c…` 与 tag/commit 一致）。
- 执行动作：先 `docker pull` tag 并核验 label 取摘要，再执行 `/var/tmp/aether-rid/update-108.sh`（由 update-106.sh 仅改摘要/版本号派生）：核对 `.env` 当前 `APP_IMAGE` 为 .106 摘要 → 备份 `.env` → 改写 `APP_IMAGE` → `docker compose -f docker-compose.yml config --images app` 核对 → `docker compose -f docker-compose.yml up -d --no-deps --wait --wait-timeout 180 app`。日志 `/var/tmp/aether-rid/update-108.log`。
- `.env` 更新前备份：`.env.bak.20260905_102716_pre_v0.7.108`，SHA256 `b18e0193dd358cf270d365e378f49b1f67f2657f54108dde94d608544695f1ac`（与 .106 更新后的 `.env` 一致）；更新后 `.env` SHA256 `c788c599dc85ba70128660de655cf3c423f3936fd3d40e02ce3a3ee54ec919da`；diff 确认仅 `APP_IMAGE` 一行变化。
- 提交边界：工作区里另有两处不属于本次发布、也不是本会话改的文件（`apps/aether-gateway/src/codex_ws/candidate_lifecycle.rs`、`codex_ws/session.rs`，09:42 UTC 被修改，内容是 WS 传输层失败不再罚账号 `penalize_account=false`），**未纳入** `f8e762f9c`，仍留在工作区未提交；其余既有脏文件（`docker-compose.yml`、`.env.bak.*`、`.gopath/`、本文件等）同样未动。

### 容器版本变化

| 项目 | 更新前 | 更新后 |
|---|---|---|
| app 版本 | `backend-v0.7.106` | `backend-v0.7.108` |
| app revision | `bed5f7e2dec7ba6c0745c5f1c20e879c893be02f` | `f8e762f9c2612f7f90af156797868472b954772c` |
| app 镜像 | `sha256:19e7aba619260739ab6716adc57ef981995743524581e52b81d7db3b5e2d0000` | `sha256:b3364675cdc7f0dc26956cd3141a5a371dc6a4db525b44e37ad4d68858fa0680` |
| app 容器 ID | `dc1b2854b9bca49f2d86c38c0bed1a42e7edf1280a99a11ef053bbce997b323c`（started 2026-09-05T06:40:06Z） | `3a72c0ceadf53aec5809e15ac623e6916ea7cab4290985d9cf60187e64e882b2`（created 2026-09-05T10:27:17Z，started 2026-09-05T10:27:28Z） |
| app 状态 | running / healthy / restart 0 | running / healthy / restart 0 |
| PostgreSQL | `postgres:15` `1c3b01479a8d…` healthy，started 2026-08-21T02:35:24Z | 未触碰，容器 ID / 启动时间 / 重启次数（0）不变 |
| Redis | `redis:7-alpine` `94725b145b2c…` healthy，started 2026-06-27T09:59:59Z | 未触碰，容器 ID / 启动时间 / 重启次数（0）不变 |

### 更新后验证

- Compose 健康等待成功（`Container aether-app Healthy`）；容器内 `/opt/aether/current/bin/aether-gateway --healthcheck` 退出码 `0`；`http://127.0.0.1:20123/health` 返回 HTTP `200`。
- 启动日志：`auto database preparation enabled` 后 0.1s 内 `gateway_ready`，无 migration/backfill 执行日志。更新前后 `_sqlx_migrations` 均为 `55` 成功、`0` 失败，最新 `20260827000000`；`schema_backfills` 均为 `5`，最新 `20260616120000`。
- `backend-v0.7.106..backend-v0.7.108` 改动 10 个文件（gateway 5 个 Rust 源、`resources/codex-client-header-profiles.json`、前端 2 个、docs 2 个），无 migration/backfill、Compose 或环境变量定义变化；未执行数据库 schema/data 变更，因此未创建新的数据库备份。
- 启动后前 2 分钟 WARN 13 条、之后 10 分钟累计 285 条，均与本次改动无关：`stream::execution`/`sync::execution` 候选重试 157 条（第三方「刀刀刀」503 /「云烟」429，`gpt-5.6-luna` 每请求各触发一次）、`dispatch::pool_scheduler` `pool_group_exhausted` 62 条（provider 全部为「Codex Free」，2 个 key 均 `pool_account_exhausted`，该 provider 近 24h 用量 0，.106 记录里已是既有状态）、`http_request_failed` 32 条（第三方 502/403 后候选耗尽）、`model_fetch::runtime` 2 条（第三方 provider 提示）；无 ERROR、无 `panic`/`fatal`、无任何 `codex_rid_*` 事件（`codex_rid_thread_reused` 为 debug 级，INFO 日志不会出现）。
- 上线后 11 分钟 Codex Pro 稳定每分钟约 30 条 200（累计 313 条 200、3 条 499、2 条 503），Codex Plus 25 条 200。
- runbook §3.1（`-v label=v108 -v since='2026-09-05 10:27:28+00'`，10:34 UTC 取数）：144 条改写请求 `official4=144`、`short_hdr=0`，`miss_pct=3.5`、`cached_share=95.2`（优于 .105 基线 6.9% / §3.4）。
- runbook §7 `version` 头：144 条中 `has_version=144`、`version_matches_ua=144`（.106 为 0%，.107 内容生效）。
- runbook §3.5 `review.sql`：154 条改写请求，Q2 `wn_without_ctx=0`、`header_vs_blob_window_mismatch=0`；Q3 无 `leak:*`、`session!=thread=0`；Q4 出站身份头恰为 8 种（四个 dash 头 + turn-metadata + beta-features + originator + installation-id），无 `session_id`/`conversation_id`/`x-trace-id`；Q6 13 条出站 thread 全部 v7 形状合规；Q8 `out_threads=13`（入站 15）；Q9/Q10 均 0 行（Codex Pro 全部请求已改写）。Q2 `window_id_mismatch=22` 为**既有**入站形状而非回归：.106 时段同口径 4030 条中 579 条，两段里该形状全部是入站与出站都没有 `window_number` 键、`window_id` 后缀原样保留、头与 blob 一致。
- 按账号每日出站 thread（§3.5 末尾 SQL）：5 个活跃账号 `out_threads` 分别 4/4/3/1/1（入站 4/5/3/2/1），远低于 32 配置抖出的 16–32 额度；.108 到达顺序 mint 正常。
- Redis 新键形已出现：`ap:*:codex_rid:*:threads` 名册 5 个（type zset、zcard 3–4、TTL ≈26h，与设计一致），`…:thread:*` 序号键 183 个（含 .106 遗留同名哈希槽键；切换当天序号若撞上遗留键会跳过该序号，属文档已写明的一次性瞬态）。只统计键名与数量，未读值。

### 本次发布内容

- `f8e762f9c`：Codex 号池会话身份合成——
  - （.108）新对话按**到达顺序**各开一条 thread：`{day}:thread:{index}` 用 `SET NX` 按序号认领（并发下精确不超每日额度），`{day}:threads` ZSET 名册按「最近一次新 turn 时间」计分；当天额度用满后复用最久没有新 turn 的那条；跨日延续的 root 在下一个新 turn 时计入当天名册但不占序号；根 freeze 竞争失败时把误 mint 的 thread 从名册撤回。删除原按 root 哈希取槽的 `thread_slot`（同一忙客户端不再呈现为固定一组 thread）。新增 debug 事件 `codex_rid_thread_reused`。
  - （.107 内容，未单独发版）每日 thread / turn 上限改为天花板：当天实际额度按账号、按天（turn 再按 thread）在 `[ceil(N/2), N]` 内确定性抖动；内置 UA 字典换成 23 组线上真实 0.153.x；所有写出站 UA 的表面（HTTP /responses、compact、WS 握手、Search）同时把 `version` 头写成 UA 的版本，不再透传入站值。
  - 前端「会话身份合成」卡片文案改为「每日上限 + 到达顺序 / 复用最久未用」；设计文档 §7.1/§8/§18.15/§18.16、运维手册同步。
- 测试：`codex_runtime_identity`/`codex_profile`/planner 定向 62 passed / 0 failed（`/var/tmp/aether-rid/cargo-test-108.log`），并发 mint 测试连跑 6 次无抖动；vitest `poolAdvancedDialog.spec.ts` 9/9；CI `Rust release tests` 通过。

### 回滚参考

- 上一版本精确回滚镜像：`ghcr.io/shirtiny/aether@sha256:19e7aba619260739ab6716adc57ef981995743524581e52b81d7db3b5e2d0000`（`backend-v0.7.106`）。
- 如需回滚，应恢复 `.env.bak.20260905_102716_pre_v0.7.108` 中的 `APP_IMAGE`，再仅重建 `app`；须另行明确授权。
- 本次无数据库迁移或数据变更，回滚不需要数据库降级。Redis 新增的 `…:{day}:threads` ZSET 与按序号语义使用的 `…:{day}:thread:{index}` 键在回滚后会被 .106 忽略或按哈希槽语义读取（值同为 thread UUID，形状兼容），随 TTL 自然过期，无需清理。
- 回滚到 .106 会同时退掉 .107 内容（`version` 头回到 0%、每日上限回到硬模数），且线上 Codex Pro 自定义 UA 已是 0.153.x 列表，不受回滚影响。

## 2026-09-05 15:28:47 UTC - 更新完成 / backend-v0.7.109

- 授权说明：用户指示「先把线上更新到最新版本」（同一条消息里另要求为 codex `context_management` 功能写支持计划到 docs，不含代码实现）。本次仅更新 Aether `app` 服务，未更新、重启或重建 PostgreSQL、Redis、sub2api 及其他容器。
- 目标版本：Git tag `backend-v0.7.109`，commit `fb8dc9ee0f50ad13022e404a714fbcfe66a37531`（`custom` 分支，14:05 UTC 授权后已推送）；镜像由 tag 推送触发的 `Release Aether` 工作流构建，`api.github.com` 公开接口取到 run id `33971077114`，14:11 UTC 开始、14:48 UTC 全部 success。
- 目标镜像：`ghcr.io/shirtiny/aether@sha256:39b1e8c621ebf3e191e27f1cbbf03bf1f119f6f3f0b76044b7c92095a3819a93`（linux/amd64 固定摘要，镜像 created 14:48:00 UTC，label version=`backend-v0.7.109`、revision=`fb8dc9ee0…` 与 tag/commit 一致）。
- 执行动作：`/var/tmp/aether-rid/update-109.sh`（由 update-108.sh 仅改摘要/版本号派生）：核对 `.env` 当前 `APP_IMAGE` 为 .108 摘要 → 备份 `.env` → 改写 `APP_IMAGE` → `docker compose -f docker-compose.yml config --images app` 核对 → `docker compose -f docker-compose.yml up -d --no-deps --wait --wait-timeout 180 app`。日志 `/var/tmp/aether-rid/update-109.log`。
- `.env` 更新前备份：`.env.bak.20260905_152830_pre_v0.7.109`，SHA256 `c788c599dc85ba70128660de655cf3c423f3936fd3d40e02ce3a3ee54ec919da`（与 .108 更新后的 `.env` 一致）；更新后 `.env` SHA256 `5e2bc707e6d6e5ecf2265715a02ad3cb2530ad9cdd16f4eebb941e1ba23caab1`；diff 确认仅 `APP_IMAGE` 一行变化。
- 提交边界：工作区里的既有脏文件（`docker-compose.yml` 的 `dns:` 三条、`.env.bak.*`、`.gopath/`、两份别人的 docs、本文件）均未动、未纳入 `fb8dc9ee0`。`docker-compose.yml` 的 `dns:` 改动在 .108 容器上就已生效（旧容器 inspect 里已有同样三条 DNS），本次重建没有引入新的运行时差异。

### 容器版本变化

| 项目 | 更新前 | 更新后 |
|---|---|---|
| app 版本 | `backend-v0.7.108` | `backend-v0.7.109` |
| app revision | `f8e762f9c2612f7f90af156797868472b954772c` | `fb8dc9ee0f50ad13022e404a714fbcfe66a37531` |
| app 镜像 | `sha256:b3364675cdc7f0dc26956cd3141a5a371dc6a4db525b44e37ad4d68858fa0680` | `sha256:39b1e8c621ebf3e191e27f1cbbf03bf1f119f6f3f0b76044b7c92095a3819a93` |
| app 容器 ID | `3a72c0ceadf53aec5809e15ac623e6916ea7cab4290985d9cf60187e64e882b2`（started 2026-09-05T10:27:28Z） | `376655eaa9e8619e7121ebda4cd7efcd4e1916a24711c47020eded0ca902ea80`（created/started 2026-09-05T15:28:41Z） |
| app 状态 | running / healthy / restart 0 | running / healthy / restart 0 |
| PostgreSQL | `postgres:15` `1c3b01479a8d…` healthy，started 2026-08-21T02:35:24Z | 未触碰，容器 ID / 启动时间 / 重启次数（0）不变 |
| Redis | `redis:7-alpine` `94725b145b2c…` healthy，started 2026-06-27T09:59:59Z | 未触碰，容器 ID / 启动时间 / 重启次数（0）不变 |

### 更新后验证

- Compose 健康等待成功（`Container aether-app Healthy`）；容器内 `--healthcheck` 退出码 `0`；`http://127.0.0.1:20123/health` 返回 HTTP `200`。
- 启动日志无 migration/backfill 执行；更新前后 `_sqlx_migrations` 均为 `55` 成功、`0` 失败，最新 `20260827000000`；`schema_backfills` 均为 `5`，最新 `20260616120000`。
- `backend-v0.7.108..backend-v0.7.109` 改动 11 个文件（+906/−153：gateway Rust 源、设计文档 §18.17、运维手册），无 migration/backfill、Compose 或环境变量定义变化；未执行数据库 schema/data 变更，因此未创建新的数据库备份。
- 启动后前 3 分钟 WARN 14 条，到 15:52 UTC 累计 36 条、ERROR 0，均与本次改动无关：候选重试 35 条（`stream::execution` 28 + `sync::execution` 7，第三方 provider 可重试状态）、候选 watchdog 超时 4 条、pending 请求清理 3 条、其余单条；无 `panic`/`fatal`、无任何 `codex_rid_*` 事件。
- 上线后 ~20 分钟 Codex Pro 641 条 200、5 条 499、2 条 503（与 .108 同期比例一致）。
- runbook §3.1（`-v label=v109 -v since='2026-09-05 15:28:41+00'`，15:52 UTC 取数）：有入站 blob 的 663 条 `official4=663`、`short_hdr=0`、`miss_pct=2.9`、`cached_share=95.9`（.108 为 3.5 / 95.2）；无入站 blob 的合成请求 225 条 `official4=225`、`short_hdr=0`、`miss_pct=2.2`、`cached_share=77.2`。
- runbook §7 `version` 头：609 条中 `has_version=609`、`version_matches_ua=609`。
- runbook §3.5 `review.sql`（15:46 UTC）：443 条改写请求。Q2 `has_wn=443`、`wn_matches_window_id=443`、`has_ctx=443`、`wn_without_ctx=0`、**`window_id_mismatch=0`**（.108 为 22/154，本次 blob 整体重建的目标项）、`header_vs_blob_window_mismatch=0`；Q1 出站 blob 键集与 0.153 客户端 turn 形状一致（17 个固定键 + `turn_trigger`/`workspace_kind`/`workspaces`/`compaction` 按入站有无）；Q3 `agent_name=/root` 443、`thread_source=user` 443、`request_kind` turn 432 / compaction 5 / prewarm 6、无 `leak:*`、`session!=thread=0`；Q4 出站身份头恰为 8 种；Q5 26 条出站 thread，其中 6 条在窗口内经历 compaction 后 `window_number` +1 且 `context_window_id` 换新；Q6 26/26 v7 形状合规；Q8 `out_threads=26`（入站 43）。
- Q9 本次出现 182 行「no inbound blob, outbound blob」，**是 .109 的预期变化而非回归**：入站没有 `x-codex-turn-metadata` 的请求（cafecode 剥掉 codex 头的 codex UA 154 条、带 `session_id` 头的非 codex 客户端 72 条）在 .108 只补四个官方头、不带 blob（.108 同口径窗口 126 条「no_in_blob/no_out_blob」——真实客户端不会有「有 thread 头没有 turn-metadata」的形状），.109 起按 0.153 客户端形状生成完整 17 键 turn blob：`request_kind=turn` 237/237、`agent_name=/root` 237/237、`window_id` 后缀 == `window_number` 237/237、11 条出站 thread。另有 74 条审计行两侧都没有捕获到请求头（66 条 200、5 条 499、1 条 502、2 条无状态），属 body capture 未覆盖的路径，与 .108 相同。
- sandbox 跟随出站 UA 操作系统（.109 目标项二）：turn/compaction/prewarm 出站 blob 里 Linux UA 只出现 `none`/`seccomp`（129/45）、Mac UA 只出现 `none`/`seatbelt`（33/176）、Windows UA 只出现 `none`/`windows_elevated`/`windows_sandbox`（292/271/61），无跨操作系统组合（.108 前 3 天有 171 条 Mac/Ubuntu UA 配 Windows 沙箱标签）。

### 本次发布内容

- `fb8dc9ee0`：Codex 号池会话身份合成——turn / compaction / prewarm 的出站 `x-codex-turn-metadata` blob 由 `request_identity_blob` 按 codex-rs 0.153 `CodexTurnMetadataPayload` 的键顺序整体重建（补 `agent_name` / `window_number` / `context_window_id` / `sandbox_mode` / 三个 review 标志；`turn_started_at_unix_ms` 缺失时补出站 turn 的 v7 时间戳，prewarm 不补）；`sandbox` 在所有非 memory blob 上按出站 UA 操作系统投影（none/external 保持；Windows 保留 `windows_sandbox`；其余按 OS 映射到 `seatbelt` / `windows_elevated` / `seccomp`）；`request_kind` 缺失或未知时仍只改已存在键。起因：.108 上线复核 Q2 `window_id_mismatch` 22/154 全部来自 codex-tui 0.146/0.147 入站不带 `window_number`，而出站 UA 是 0.153.x；以及 Mac/Ubuntu UA 配 Windows 沙箱标签。设计文档 §18.17、运维手册 §3.5 Q11/Q12 同步。
- 测试：`codex_runtime_identity`/`codex_profile`/planner 定向 82 passed / 0 failed（`/var/tmp/aether-rid/cargo-test-109.log`）；CI `Rust release tests` 通过。

### 回滚参考

- 上一版本精确回滚镜像：`ghcr.io/shirtiny/aether@sha256:b3364675cdc7f0dc26956cd3141a5a371dc6a4db525b44e37ad4d68858fa0680`（`backend-v0.7.108`）。
- 如需回滚，应恢复 `.env.bak.20260905_152830_pre_v0.7.109` 中的 `APP_IMAGE`，再仅重建 `app`；须另行明确授权。
- 本次无数据库迁移或数据变更，回滚不需要数据库降级；Redis 键形与 .108 相同，无需清理。回滚后合成请求会退回「四个官方头、无 blob」与「沙箱标签原样透传」的形状。

## 2026-09-06 17:53:48 UTC - 更新完成 / backend-v0.7.110

- 授权说明：用户指示「远端 actions 成功了吗 更新线上容器到最新版本」，随后「再试试」明确授权直接执行更新.本次仅更新 Aether `app` 服务，未更新、重启或重建 PostgreSQL、Redis、sub2api 及其他容器.
- 目标版本：Git tag `backend-v0.7.110`，commit `90e0ea87e706a4c00d6f88c7ecc010645578be9f`（`custom` 分支）；镜像由 tag 推送触发的 `Release Aether` 工作流构建，`api.github.com` 公开接口取到 run id `34045185053`，2026-09-06 16:21:41 UTC 开始、16:58:44 UTC 全部 success.
- 目标镜像：`ghcr.io/shirtiny/aether@sha256:4d1e84ec0ff9f5616c7984b287dac55e16c2b2d6daf07202837a3c42d338ffae`（linux/amd64 固定摘要，label version=`backend-v0.7.110`、revision=`90e0ea87e…` 与 tag/commit 一致）.摘要由 `backend-v0.7.110` release 附带的 `image-build-backend-v0.7.110.json` 取得并校验为 `sha256:[0-9a-f]{64}`.
- 执行动作：`/var/tmp/aether-rid/deploy-v0.7.110.sh`（PRE-STATE inspect → 取摘要并校验 → 备份 `.env` → `sed -i -E` 仅改 `APP_IMAGE` 一行 → diff 核对 → `docker compose -f docker-compose.yml config` 核对解析镜像 → `docker compose -f docker-compose.yml up -d --no-deps --wait --wait-timeout 180 app` → POST-STATE inspect + `/health` + 迁移计数）.日志 `/var/tmp/aether-rid/update-110.log`.
- `.env` 更新前备份：`.env.bak.20260906_175335_pre_v0.7.110`，SHA256 `5e2bc707e6d6e5ecf2265715a02ad3cb2530ad9cdd16f4eebb941e1ba23caab1`（与 .109 更新后的 `.env` 一致）；更新后 `.env` SHA256 `b92148733e5072d072086b6870fa9f458c69fb7cd274108817fdc7a5eb58c2dc`；diff 确认仅 `APP_IMAGE`（第 31 行，`39b1e8c6…` → `4d1e84ec…`）一行变化.
- 提交边界：工作区里的既有脏文件（`docker-compose.yml` 的 `dns:` 三条、`.env.bak.*`、`.gopath/`、若干别人的 docs、本文件）均未动、未纳入 `90e0ea87e`.本次重建未引入新的运行时差异.

### 容器版本变化

| 项目 | 更新前 | 更新后 |
|---|---|---|
| app 版本 | `backend-v0.7.109` | `backend-v0.7.110` |
| app revision | `fb8dc9ee0f50ad13022e404a714fbcfe66a37531` | `90e0ea87e706a4c00d6f88c7ecc010645578be9f` |
| app 镜像 | `sha256:39b1e8c621ebf3e191e27f1cbbf03bf1f119f6f3f0b76044b7c92095a3819a93` | `sha256:4d1e84ec0ff9f5616c7984b287dac55e16c2b2d6daf07202837a3c42d338ffae` |
| app 容器 ID | `376655eaa9e8619e7121ebda4cd7efcd4e1916a24711c47020eded0ca902ea80`（started 2026-09-05T15:28:41Z） | `4f770553aa46e675aa2b321974845404d1b254df8fd03a81e94eef9120abd8e2`（created/started 2026-09-06T17:53:48Z） |
| app 状态 | running / healthy / restart 0 | running / healthy / restart 0 |
| PostgreSQL | `postgres:15` `1c3b01479a8d…` healthy，started 2026-08-21T02:35:24Z | 未触碰，容器 ID / 启动时间 / 重启次数（0）不变 |
| Redis | `redis:7-alpine` `94725b145b2c…` healthy，started 2026-06-27T09:59:59Z | 未触碰，容器 ID / 启动时间 / 重启次数（0）不变 |

### 更新后验证

- Compose 健康等待成功（`Container aether-app Healthy`）；`http://127.0.0.1:20123/health` 返回 HTTP `200`；容器 `running` / `healthy` / restart 0.
- 启动日志（`docker logs --since` 新容器启动时刻，88 行）：`ERROR 0`、`panic 0`、`WARN 4`（均为第三方 provider 既有问题：`刀刀刀` 503 候选重试、`云烟` 429、openlux 账号迁移提示、某分组停用），无任何 `codex_rid_*` 事件、无 migration/backfill 执行.
- 更新前后 `_sqlx_migrations` 均为 `55` 成功、`0` 失败，最新 `20260827000000`；`schema_backfills` 均为 `5`，最新 `20260616120000`.迁移清单与 backfill 清单在 .109→.110 之间字节一致，本次为纯 app 更新，未执行数据库 schema/data 变更，因此未创建新的数据库备份.
- `backend-v0.7.109..backend-v0.7.110` 改动 7 个文件（+1323/−498：`codex_runtime_identity.rs`、`aether-runtime-state` 三个文件、`PoolAdvancedDialog.vue`、设计文档 §18.18、运维手册），无 migration/backfill、Compose 或环境变量定义变化.
- 号池上限沿用 .109 时的线上配置（Codex Pro 16 thread / 128 turn），本次未改动阈值；.110 把 turn 上限改为账号级近 24h 滚动硬顶并对名册/台账键改名，切换当天忙账号可见 thread 数可能一次性略超上限，约 24–36h 随键 TTL 自然收敛（符合运维手册 §4.2 过渡说明，过渡期 thread 上限 16 ≥ 建议下限）.
- 待办（未在本次执行、需另行取数确认）：运维手册 §4.1/§4.2 出站形状复核（app-server-only blob 键仅在 codex_vscode originator 下保留、turn 不回访、账号级 24h turn 计数不超上限），需积累一段真实流量后按 `review.sql`/`per_account_threads.sql` 取数.

### 本次发布内容

- `90e0ea87e`：Codex 号池会话身份合成四处形状缺陷修复——
  - **turn 上限改为账号级、最近 24h 滚动硬顶**：新增 turn 台账 ZSET `{prefix}:turns`（成员=出站 turn UUID、分数=mint 秒），thread 名册 `{prefix}:threads` 同样去掉 `:{day_id}` 改滑动窗；准入走单条 Redis Lua `score_add_if_count_below`（`ZREMRANGEBYSCORE -inf (window_start` 剪枝 + `ZCOUNT` ≥ 上限即拒绝，内存后端同构）；thread mint 与 turn 预算耦合，故 distinct thread ≤ min(thread 上限, turn 上限).
  - **一条出站 thread 只有一个 open turn**（`{prefix}:open:{thread}`）：被取代的旧入站 turn 回放时 Steer 向前、不回访 turn id；WS 绑定连接对自己在飞 turn 永远 Snapshot 权威；turn-state 仅在 `forwards_turn_state()` 时转发.
  - **上限硬顶可低至 1**（`MIN_THREADS_PER_DAY=MIN_TURNS_PER_DAY=1`，前端范围 HEAD 即 min:1）——thread 可 <8、turn 可 <64，账号每日出站 turn ≤ turn 上限，远低于官方约 100 的风控线.
  - **`BLOB_APP_SERVER_KEYS=["turn_trigger","workspace_kind"]` 只在 app-server（codex_vscode）originator 下保留**，终端/无头出站 UA 从 blob 删——修正 .109 §18.17「按白名单原样搬」.
  - 存储层新增 `score_add_if_count_below`（`aether-runtime-state` memory.rs + redis/runtime.rs，契约测试 `runtime_backends_share_kv_score_and_queue_contracts` 覆盖两后端）；新 debug 事件 `codex_rid_turn_steered` / `codex_rid_turn_budget_exceeded`.
- 测试：`cargo test -p aether-gateway codex_runtime_identity` 40 passed / 0 failed（含 `account_ceilings_hold_below_the_official_thresholds`、`no_turn_id_revisit_within_a_thread_under_interleaved_roots`、`turn_ceiling_of_one_mints_exactly_one_turn`、`app_server_only_blob_keys_survive_only_under_an_app_server_user_agent` 等 14 项新/改测试），runtime-state 契约测试通过；CI run `34045185053` `Release Aether` 全部 success.

### 回滚参考

- 上一版本精确回滚镜像：`ghcr.io/shirtiny/aether@sha256:39b1e8c621ebf3e191e27f1cbbf03bf1f119f6f3f0b76044b7c92095a3819a93`（`backend-v0.7.109`）.
- 如需回滚，应恢复 `.env.bak.20260906_175335_pre_v0.7.110` 中的 `APP_IMAGE`，再仅重建 `app`；须另行明确授权.
- 本次无数据库迁移或数据变更，回滚不需要数据库降级.Redis 里 .110 新增的 `{prefix}:turns` / `{prefix}:open:{thread}` 及去 `:{day_id}` 的 `{prefix}:threads` 键在回滚后会被 .109 忽略（.109 用 `:{day_id}:threads` 名册与按序号哈希槽语义），随 TTL 自然过期，无需清理；回滚后账号级 24h turn 硬顶退回每出站 thread 的 turn 模数、app-server-only blob 键退回按白名单原样搬.

## 2026-09-07 23:43:26 UTC - 更新完成 / backend-v0.7.114

- 授权：用户在询问「允许仅更新 Aether app 到 backend-v0.7.114 吗？」后明确回复「更新」。此前被安全审查阻止的部署未执行；本次得到明确授权后完成一次直接更新，无灰度。
- 发布提交：`9873935647867f509617d9d3cd300eea3f1facca`（`custom` 分支），tag `backend-v0.7.114`；tag push 触发 [Release Aether / 34144273753](https://github.com/Shirtiny/Aether/actions/runs/34144273753)，预检、Rust 测试、前端、linux-amd64、tarball、Docker 镜像和 Release 附件全部 success。
- 固定镜像：`ghcr.io/shirtiny/aether@sha256:25b871e7aefb58f27786c8e8b6491c28db620ad3842eefb630d8be39a27ea0c9`，拉取后核对 linux/amd64、version=`backend-v0.7.114`、revision=`987393564…` 与远端构建记录一致。
- 更新前：`backend-v0.7.113` / `2ca74f348510941070da1493b00010ab43ac9fda`，镜像 `sha256:62b3b24f6eeecbceaaad2286235ac3a6872734a75c0b7e9d1f665672d8c15e51`；app 容器 `cf77211393681ee14c8bed1e8c29376b3240255fdc4f9e73505e791b253515c5`，healthy / restart 0。
- 更新后：app 容器 `8fe7aa58e7d885b8d890e477145077690133b05d99f94f6011e411bbf38c65d2`，started=`2026-09-07T23:43:26.67449525Z`；Compose 健康等待成功，`http://127.0.0.1:20123/health` 多次返回 200，观察结束仍 healthy / restart 0。
- 仅拉取指定摘要、备份并改写 `.env` 的 `APP_IMAGE`，仅执行一次 `docker compose -f docker-compose.yml up -d --no-deps --no-build --pull never --wait --wait-timeout 180 app`。没有更新/重启 PostgreSQL、Redis、sub2api 或其他服务；三者容器 ID、镜像、启动时间、重启次数在更新前/后/观察结束完全一致。
- `.env` 备份：`.env.bak.20260907_234154_pre_v0.7.114`（权限 0600）；更新前 SHA256=`b3eced68a82a2af4c764eef09608a64f6045fda492af32558a0644e3e0ad0ac9`，更新后=`a6a67048d9cd2138b122d91e4fd736db308a28385cf24e7885134816a72f6dde`；程序核对除 `APP_IMAGE` 外逐行一致。既有 `docker-compose.yml` 三条 DNS 原本就已在 .113 容器生效，本次不改动该文件或其他用户工作区文件。
- 执行记录：`/var/tmp/aether-overload/deploy-backend-v0.7.114.log`。原脚本在拉取和环境文件改写后发现 `docker compose config --images app` 会同时输出依赖镜像，校验停止（尚未重建）；改用 JSON 精确检查 `services.app.image` 后通过 `finish-deploy-114.sh` 继续同一次更新，没有再次拉取或重复重建。

### 数据库与验证

- `_sqlx_migrations` 更新前/后/观察结束均为 55 成功、0 失败、最新 `20260827000000`；`schema_backfills` 均为 5、最新 `20260616120000`。源码及 .113/.114 Release 记录的迁移和回填摘要完全相同。
- app 启动时执行既有自动数据库准备检查，无新迁移或回填，本次没有数据库 schema/data 操作或新数据库备份。
- 本地专项测试（套件存在重叠，不作加总）：`overload_retry` 15、`execution_runtime::stream` 94、`execution_runtime::sync` 13、`orchestration` 131、`prefetch` 21，全部通过；远端 release 新增重试与 prefetch 门禁亦通过。
- 方案：[Session-preserving overload retry](docs/architecture/overload-retry-2026-09-07.md)。Aether 对指定容量拒绝最多同计划/同账号重试两次，保留 sticky；暂存无内容的 SSE 开场，第一条完整内容 data 行即释放，不等整段响应或尾部空行；正文/推理/工具启动后不重放。对断开请求停止预输出重试，耗尽重试仅发一次终止错误。HTTP/SSE 路径适用，本次不实现原生 WebSocket 重放。

### 线上观察（23:43:26–23:57:35 UTC，约 14 分钟）

- 五轮每两分钟健康检查全部 200，app 一直 healthy / restart 0。应用日志 INFO 698 / WARN 63 / ERROR 0，panic 0；没有目标过载重试/恢复/耗尽事件。
- 窗口内已完成成功请求 96（200），失败 13（400×4、429×1、503×5、504×3），另有流式在途 2。**并非所有线上请求均成功。**
- 失败分别为 G-aisc 上游请求拒绝 4、限流 1、暂不可用 1、流无新输出 120 秒超时 3；ViviApi 上游 502 经过候选耗尽返回 503 共 2；星辰ai 上游 503 共 2。没有这次指定的 `Our servers are currently overloaded...` 错误。
- 三条进度超时之前，日志记录已转发的客户端数据分别为 2,386,911 / 177 / 91,661 字节；开场重试已退出，不应强行重放。发布前 G-aisc 同类 120 秒超时和 ViviApi 502 已存在。外部供应商拒绝/限流/5xx 保留既有错误处理，本次未更改其服务或配置。
- **验证边界：** 未采集到线上真实目标过载样本，不能宣称线上重试已恢复了请求或据此计算恢复率；相关行为及首内容不加固定等待由本地真实 HTTP 故障注入、虚拟 47 秒开场、内容行即时释放测试和远端 CI 验证。线上观察证明新版本健康承载真实流量，不证明所有第三方错误已消除。
- 证据：`/var/tmp/aether-overload/{pre-deploy.txt,post-deploy.txt,final-health.txt,observation-114.log,final-traffic.txt,post-release-final.log,observation-summary.json}`。

### 回滚参考（未执行）

- 上一稳定镜像：`ghcr.io/shirtiny/aether@sha256:62b3b24f6eeecbceaaad2286235ac3a6872734a75c0b7e9d1f665672d8c15e51`（.113）。如需回滚须另行明确授权，仅恢复 APP_IMAGE 并重建 app，不需数据库降级。


## 2026-09-08T07:17:32.374509+00:00 - 更新完成 / backend-v0.7.115

- 授权：用户明确要求“看下远端 actions 是否成功，成功了的话就更新一下”。两端指定提交的发布构建、Sub2API CI 与安全扫描全部成功后执行；未移动 tag，未进行灰度或无关升级。
- Actions：[发布构建](https://github.com/Shirtiny/Aether/actions/runs/34194235469) success；版本 `backend-v0.7.115`，revision `f2ffbdaed7a58e86c833c7e2bd8d7d881b73cfc1`。
- 更新前：`backend-v0.7.114` / `9873935647867f509617d9d3cd300eea3f1facca`；镜像 `ghcr.io/shirtiny/aether@sha256:25b871e7aefb58f27786c8e8b6491c28db620ad3842eefb630d8be39a27ea0c9`；容器 `8fe7aa58e7d885b8d890e477145077690133b05d99f94f6011e411bbf38c65d2`，healthy / restart 0。
- 更新后：镜像 `ghcr.io/shirtiny/aether@sha256:5bbf4e50416c86c622c21e8634b0734d65ab0fe575e9cfffa7eb48e5f9957b36`；容器 `bd8ffef6a0b713b97f89ad6b19cb662606fdad193ce60ae589b94b7b2d87076d`，started `2026-09-08T07:17:26.118896044Z`；healthy / restart 0。
- 只改目标应用的镜像引用；配置备份 `/opt/stacks/aether/.env.bak.20260908T071714Z_pre_v0.7.115`。文件 SHA256：`a6a67048d9cd2138b122d91e4fd736db308a28385cf24e7885134816a72f6dde` → `383439192d16676c733e1abdb18a544452358db801f14c733bc1e08ac21125f8`；校验其余文件内容及渲染后的 Compose 配置不变。
- 仅重建目标应用：`docker compose ... up -d --no-deps --no-build --pull never --wait --wait-timeout 180 app`。本次发布先更新 Sub2API .74，再更新 Aether .115；未更新/重启两套 PostgreSQL、Redis 或反向代理，旧镜像未清理。
- 本地健康检查 HTTP 200；Sub2API 公网健康检查 HTTP 200。更新前后迁移/回填清单校验一致：Aether 55 个 migration（0 失败，最新 20260827000000）、5 个 backfill；Sub2API 225 个 migration（最新 196_subscription_user_quota_reset.sql）、5 个已识别回填迁移；Atlas 1 条 revision、0 不完整/失败。无新增 schema/data 迁移或回填，因此未执行数据库降级或新建数据库备份。
- 更新后短窗口两端 panic 0、容器 restart 0。已在 Aether 真实流量中核实至少 4 条过载重试后的请求最终 `completed / 200`，不是仅依据 `retry_recovered` 日志；同时仍有持续过载失败，不能据短窗口推算整体恢复率。Sub2API 本轮观察尚未捕获补救重试事件，不能宣称已在线上验证该分支恢复。
- 证据：`/var/tmp/stream-wire-release/`（Actions、固定 digest、更新前状态、每个应用更新记录、健康与日志统计、重试最终结果）。回退参考为上述更新前镜像与配置备份；如需回滚须另行明确授权，本次未回滚。


## 2026-09-09T04:45:54.606041258Z - 更新完成 / backend-v0.7.116

- 授权：用户明确要求「更新线上容器」。两端指定提交的发布构建 success 后执行；未移动 tag，未进行灰度或无关升级。
- Actions：[发布构建](https://github.com/Shirtiny/Aether/actions/runs/34302820627) success（run=181）；版本 `backend-v0.7.116`，revision `cb65e696f8fe9d1e5c5f5cad7e5d8a89f9e8b336`。
- 更新前：`backend-v0.7.115` / `f2ffbdaed7a58e86c833c7e2bd8d7d881b73cfc1`；镜像 `ghcr.io/shirtiny/aether@sha256:5bbf4e50416c86c622c21e8634b0734d65ab0fe575e9cfffa7eb48e5f9957b36`；容器 `bd8ffef6a0b713b97f89ad6b19cb662606fdad193ce60ae589b94b7b2d87076d`，healthy / restart 0。
- 更新后：镜像 `ghcr.io/shirtiny/aether@sha256:7c6350bbce7ad7b3f5fa4486f0b620a648ccd329b44097612ab4f1b1b4fa2066`；容器 `a8334ff520c2b0772132c8323d2f69cccfb3960f187fd2703abf3f8f1751630d`，started `2026-09-09T04:45:54.606041258Z`；healthy / restart 0。
- 内容：OAuth auth 刷新、token 导入、授权、quota/reset-credit 等非请求操作改为携带与普通 Codex 池请求一致的账号级 profile `User-Agent`，通过 `ProviderOAuthTransportContext → OAuthHttpRequest → LocalOAuthHttpRequest` 串联账号级 UA，并在 `GatewayOAuthHttpExecutor.execute` 与 `execute_local_oauth_http_request` 两处注入；所选 UA 复刻 planner 的 codex profile 解析（指纹匹配 > header profile > 内置默认）。非 codex adapter 传 `user_agent: None`，注入用 `or_insert` 不覆盖 provider 自带 UA。提交 `cb65e696f`。
- 只改目标应用的镜像引用；配置备份 `/opt/stacks/aether/.env.bak.20260909T044515Z_pre_v0.7.116`（权限 0600）。文件 SHA256：`383439192d16676c733e1abdb18a544452358db801f14c733bc1e08ac21125f8` → `61fa75d146f91aea7a2c3372b2a8017920de92b672ba27ee8c3d3a5fb3dc1e0d`；校验除 `APP_IMAGE` 外逐行一致（diff 非 APP_IMAGE 行差异为 0）。既有 `docker-compose.yml` 未改动。
- 仅重建目标应用：`./update.sh`（默认 compose pull + `up -d` + health wait）。未更新/重启 PostgreSQL、Redis、sub2api 或其他服务；三者容器 ID、镜像、启动时间、重启次数在更新前/后完全一致（postgres restart 0 / 14h，redis restart 0 / 2 months）。
- 本地健康检查 `http://127.0.0.1:20123/health` HTTP 200；startup 数据库准备检查按既有方式运行，无新增迁移或回填（本次提交不含任何 migration/backfill 改动），未执行数据库降级或新建备份。
- 更新后短窗口应用日志 0 ERROR / 0 panic，容器 restart 0；已承载真实成功请求（status=200），观察窗口内有持续流量，无启动异常。
- 验证边界：本次改动仅涉及 OAuth 维护流量的 UA 头；OAuth 刷新/授权为调度任务，更新后短窗口未观测到实际 refresh 事件，因此线上 UA 指纹的实际发送需在下次 OAuth 刷新窗口或手动触发后经日志确认。本地 transport suite 351 项、`state::oauth` 5、`quota` 15、`usage` 38 全绿；`tests::control::admin::oauth` 模块下 2 个与本次改动无关的既有测试栈溢出（不触碰 codex profile/UA 逻辑），判断为既有问题，不属于本改动引入。
- 回滚参考：待授权，恢复 `APP_IMAGE` 为上述更新前镜像并重建 app，无数据库降级。

## 2026-09-09T16:24:40.368307078Z - 更新完成 / backend-v0.7.118

- 授权：用户明确要求「更新到线上最新的」。事先核实两个待发布 tag 的远端 Actions 均 success 后执行；未移动 tag，未进行灰度或无关升级。
- Actions：[backend-v0.7.117](https://github.com/Shirtiny/Aether/actions/runs/34339571808) run=182 `Release Aether` success，revision `bde7af9d87d64794b8242a5076dbffa796c6973a`；[backend-v0.7.118](https://github.com/Shirtiny/Aether/actions/runs/34344671360) run=183 `Release Aether` success，revision `cba8d5b6a95509a88980957aeba3cc26619068c7`。本次直接更新到最新的 v0.7.118（包含 v0.7.117 全部改动）。
- 更新前：`backend-v0.7.116` / `cb65e696f8fe9d1e5c5f5cad7e5d8a89f9e8b336`；镜像 `ghcr.io/shirtiny/aether@sha256:7c6350bbce7ad7b3f5fa4486f0b620a648ccd329b44097612ab4f1b1b4fa2066`；容器 `a8334ff520c2b0772132c8323d2f69cccfb3960f187fd2703abf3f8f1751630d`。
- 更新后：镜像 `ghcr.io/shirtiny/aether@sha256:9ecb62a15d7657cfd816014fa559b175431c561bcc55ea9bd6f88e3473c8480f`（index digest，经 `docker buildx imagetools inspect` 与 GHCR token HEAD 请求交叉核对一致）；容器 `ae5d30d03c89a407d18a6336ff296333a7b8a5fcc7e0adcb750c6911bde1c94e`，started `2026-09-09T16:24:40.368307078Z`；healthy / restart 0；label `org.opencontainers.image.version=backend-v0.7.118`。
- 内容：本次镜像跨两个 tag。v0.7.117（本人改动，提交 `bde7af9d8`）：为 v0.7.116 的账号级 Codex profile UA 补充可观测性 —— 新增公开函数 `resolve_oauth_maintenance_user_agent`（`crates/aether-provider-transport/src/oauth_refresh/mod.rs`，包装既有 `codex_pool_account_user_agent` 并从 crate 根重新导出），在 `apps/aether-gateway/src/state/oauth.rs` 的「gateway manual oauth refresh starting」与「gateway local oauth execution request prepared」两条 `tracing::info!` 分别加 `codex_oauth_user_agent` / `user_agent` 字段，不改变任何请求行为，仅使实际发送的 UA 值可从日志直接核对。v0.7.118（非本人改动，提交 `cba8d5b6a`，"expose internal overload retry diagnostics"）：本轮未审阅其内部实现，随最新 tag 一并上线。
- 只改目标应用的镜像引用；配置备份 `/opt/stacks/aether/.env.bak.20260909T162316Z_pre_v0.7.118`（权限 0644）。文件 SHA256：`61fa75d146f91aea7a2c3372b2a8017920de92b672ba27ee8c3d3a5fb3dc1e0d` → `60288e9540dbdd290b87015d6f034316c07f9d3b68f1a1372231554915ef5f4c`；diff 仅 `APP_IMAGE` 一行，其余逐行一致。既有 `docker-compose.yml` 未改动。
- **过程事故（已处理）：** `./update.sh` 拉取镜像并 recreate app 后，compose 的 `depends_on: service_healthy` 门禁判定 aether-redis / aether-postgres 为 unhealthy 而拒绝启动 app（两次重试均失败），app 容器停留在 `Created`（未运行）状态。排查发现宿主机 `/tmp`（tmpfs，1.0G）已满 100%，导致 redis/postgres 的 exec 型健康检查探针报 `OCI runtime exec failed: ... no space left on device`——探针失败而非服务本身故障（两容器进程实际一直 Up，未重启）。清理 `/tmp/sub2api-modcache`（773M）、`/tmp/sub2api-gocache`（128M）、`/tmp/sub2api-clean`（57M）三个无进程占用、非本次相关的 Go 构建缓存后 `/tmp` 降到 18%；随后执行 `docker start aether-app`（绕开 compose 依赖门禁，直接启动已 Recreate 好的容器，未触碰 redis/postgres/其他容器）恢复服务。app 容器 `Created→Started` 间隔约 53 秒（16:23:47Z → 16:24:40Z），期间该容器不可用；redis/postgres 健康检查约 15 秒后自然恢复为 healthy，两者容器 ID、启动时间、重启次数全程未变（均 restart 0）。sub2api 及其他容器全程未受影响。此为既有 `/tmp` tmpfs 缓存写满问题的再次发生，与本次镜像改动无关。
- 本地健康检查 `http://127.0.0.1:20123/health` HTTP 200；数据库准备检查按既有方式运行：`_sqlx_migrations` 55 成功 / 0 失败，最新 `20260827000000`；`schema_backfills` 5 条；均与更新前一致，无新增迁移或回填，未执行数据库降级或新建备份。
- 更新后 5 分钟窗口应用日志 0 ERROR / 0 panic，容器 restart 0；已承载真实成功请求（`status_code=200`，含 `/v1/responses` 流式与过载重试恢复事件），观察窗口内有持续流量。
- 验证边界：v0.7.117 的新增日志字段本身尚未观测到真实 OAuth 刷新事件命中（该路径为调度任务/手动触发），账号级 UA 的实际发送值需在下次刷新窗口或手动触发后经 `codex_oauth_user_agent`/`user_agent` 日志字段核对；v0.7.118（overload retry diagnostics）未经本人代码审查，仅确认其 CI 通过与部署后无异常，不对其内部逻辑正确性背书。
- 回滚参考：上一稳定镜像 `ghcr.io/shirtiny/aether@sha256:7c6350bbce7ad7b3f5fa4486f0b620a648ccd329b44097612ab4f1b1b4fa2066`（.116）。如需回滚须另行明确授权，仅恢复 `APP_IMAGE` 并重建 app，无数据库降级；回滚不受本次 `/tmp` 清理影响（清理的均为可再生构建缓存）。

## 2026-09-10T00:06:18.664518047Z - 更新完成 / backend-v0.7.120

- 授权：用户明确要求「更新线上容器到最新版本」。发布构建 success 后执行；未移动 tag，未进行灰度或无关升级。
- Actions：[backend-v0.7.120](https://github.com/Shirtiny/Aether/actions/runs/34388067904) `Release Aether` success，revision `a3fdcd0a0daadbe226f0f4706481d8802d88fe8b`。tag `backend-v0.7.119`（`c91b6ae1d`）此前从未上线（线上此前一直是 .118），本次直接更新到 .120，含 .119 全部改动。
- 更新前：`backend-v0.7.118` / `cba8d5b6a95509a88980957aeba3cc26619068c7`；镜像 `ghcr.io/shirtiny/aether@sha256:9ecb62a15d7657cfd816014fa559b175431c561bcc55ea9bd6f88e3473c8480f`；容器 `ae5d30d03c89a407d18a6336ff296333a7b8a5fcc7e0adcb750c6911bde1c94e`，healthy / restart 0。
- 更新后：镜像 `ghcr.io/shirtiny/aether@sha256:252bb5e710c2765c6507c655ee390d3413d0ad48423fb2ec76729a5a5590d8c2`（index digest，经 `docker buildx imagetools inspect` 核对，与同一构建的 `latest` tag 一致）；容器 `2ea248acecf837b3bb8897ca3109d463745b29145ed2a448157e64a1666c28b8`，started `2026-09-10T00:06:18.664518047Z`；healthy / restart 0；label `org.opencontainers.image.version=backend-v0.7.120`。
- 内容：本次镜像跨三个提交。`c91b6ae1d`（.119，用户自己的提交，"remove same-plan overloaded retries"）：本人未审阅其内部实现，随最新 tag 一并上线。`7a57203f9`（本人本次改动，"match codex-rs login wire shape and pin the client identity"）：修复 Codex OAuth 登录/刷新流量与官方 codex-rs 客户端形状不一致的问题——authorize URL 改为 codex-rs 的参数顺序、scope 用 `%20` 编码并补齐 `api.connectors.read/invoke`、去掉 `prompt=login`、`originator` 放最后一个参数；`state` 改为 32 字节、PKCE verifier 改为 64 字节，均 base64url；code 换 token 改为手拼 form（不带 scope），随后无条件执行一次可容错的 api-key token exchange（复刻 CLI 登录后动作）；refresh 改为 JSON `{client_id, grant_type, refresh_token}`；所有 auth 请求补齐 `originator` + User-Agent，去掉多余的 accept 头。同时把「登录时用哪个 UA/originator」做成一条链路：授权开始时选定 → 存入 OAuth state → 换 token 时复用 → 冻结进 key 指纹，使同一账号后续的池内流量与登录时呈现同一身份；已有 key 重新授权沿用其持久化身份，导入/批量导入按账号各选一个并冻结；OAuth 维护性刷新携带账号的 UA+originator 而不只是 UA。非 codex provider 的 nonce/verifier/body 形状不变。`a3fdcd0a0`（本人，纯文档提交，记录 .118 部署）：无运行时改动。
- 只改目标应用的镜像引用；配置备份 `/opt/stacks/aether/.env.bak.20260910T000547Z_pre_v0.7.120`（权限 0644）。文件 SHA256：`60288e9540dbdd290b87015d6f034316c07f9d3b68f1a1372231554915ef5f4c` → `9b8236924ae5390dbd20f8a301a77fc28aad9c7ec929d2d38460b9379af36701`；diff 仅 `APP_IMAGE` 一行，其余逐行一致。既有 `docker-compose.yml` 未改动。
- 仅重建目标应用：`./update.sh`（默认 compose pull + `up -d` + health wait），未出现此前 `/tmp` 写满导致健康检查探针失败的问题（更新前 `/tmp` 用量 87%，本次拉取/重建期间未见异常）。未更新/重启 PostgreSQL、Redis、sub2api 或其他服务；postgres/redis 容器 ID、启动时间、重启次数在更新前后完全一致（均 restart 0）。
- 本地健康检查 `http://127.0.0.1:20123/health` HTTP 200；启动日志显示 auto database preparation 按既有方式运行，未出现新增迁移或回填日志，判断为与更新前一致，未执行数据库降级或新建备份。
- 更新后 3 分钟窗口应用日志共 58 行，0 ERROR / 0 panic，容器 restart 0。
- 本次改动的本地测试：aether-admin 159、aether-data 517、aether-oauth 41、aether-provider-transport 355（1 ignored）、gateway 定向套件 154（含 `tests::control::admin::oauth` 78）全部通过；`cargo clippy -D warnings` 在 `crates/aether-runtime-state/src/lib.rs:620` 有一个既有 `too_many_arguments` 告警（.110 引入，非本次改动，Release 工作流不跑 clippy，不影响本次发布）。
- 验证边界：OAuth 登录/刷新的新流量形状只在实际发生 authorize/refresh 时才会体现；更新后短窗口内未发生真实的 OAuth 授权或刷新事件，因此线上实际发出的 authorize URL 与 `originator`/User-Agent 头仍需在下一次账号授权或手动触发刷新后，通过日志字段核对确认，本条记录暂不作该项的线上验证结论。
- 回滚参考：上一稳定镜像 `ghcr.io/shirtiny/aether@sha256:9ecb62a15d7657cfd816014fa559b175431c561bcc55ea9bd6f88e3473c8480f`（.118）。如需回滚须另行明确授权，仅恢复 `APP_IMAGE` 并重建 app，无数据库降级。

## 2026-09-10T15:34:22.557498559Z - 更新完成 / backend-v0.7.122

- 授权：用户明确要求「更新线上容器到最新版本」。发布构建 success 后执行；未移动 tag，未进行灰度或无关升级。
- Actions：[backend-v0.7.122](https://github.com/Shirtiny/Aether/actions/runs/34492210256) `Release Aether` success，revision `671f68bad1b9220cf34db96c9e767d4ae1d3690b`。tag `backend-v0.7.121`（`eb3202cf7`）此前从未上线（线上此前是 .120），本次直接更新到 .122，含 .121 全部改动。
- 更新前：`backend-v0.7.120` / `7a57203f9`（tag `backend-v0.7.120` → `a3fdcd0a0`）；镜像 `ghcr.io/shirtiny/aether@sha256:252bb5e710c2765c6507c655ee390d3413d0ad48423fb2ec76729a5a5590d8c2`；容器 `2ea248acecf837b3bb8897ca3109d463745b29145ed2a448157e64a1666c28b8`，healthy / restart 0。
- 更新后：镜像 `ghcr.io/shirtiny/aether@sha256:0623bed790d677dcd63286d66a0979c3b9075f807e4488f0ccc53b246b5f4ad0`（index digest，经 `docker buildx imagetools inspect ghcr.io/shirtiny/aether:backend-v0.7.122` 核对；linux/amd64 子 manifest `sha256:763d354ddefab5c53fd8ec103263fccf30b34434bbefb109bde4321eee050a63`）；容器 `5305383f6f0dfa4587b949a766f7504d56254482472454de2a2d2088e113240d`，started `2026-09-10T15:34:22.557498559Z`；healthy / restart 0；label `org.opencontainers.image.version=backend-v0.7.122`。
- 内容：跨两个提交。`eb3202cf7`（.121，本人，`fix(gateway): remove internal retry diagnostics`）：移除内部重试诊断输出，无用户可见行为变更。`671f68bad`（本人本次改动，`feat(gateway): normalize codex <environment_context> timezone and date`）：新增独立 pass（`apps/aether-gateway/src/codex_environment_context.rs`），在身份合成改写之后对 HTTP `/responses`、`/compact` 与 WebSocket 每步 body 改写 `input[]` 里 `<environment_context>` 块的 `<timezone>`（= 网关容器时区：`AETHER_CODEX_ENVIRONMENT_TIMEZONE` > `TZ` > `/etc/localtime` > 保底 `America/New_York`；UTC 类名被拒、中国时区硬性禁止）与 `<current_date>`（按每个 item 自身 UUIDv7 瞬时在该时区重算）；删除被改写后变冗余的客户端 diff，并按官方 section 顺序（AGENTS.md user 消息、`<skills_instructions>` 等在 Environments 之前）合成日切 diff，UUIDv7 确定性可复现，WS 增量步靠进程内 `env_context_state` 续状态；同时删除 `tools[].user_location`。`<cwd>` / `<shell>` / `<filesystem>` 等其余字节逐字节不动。kill switch：`AETHER_CODEX_ENVIRONMENT_CONTEXT_REWRITE=off`。事件：`codex_env_tz_resolved` / `codex_env_tz_rejected` / `codex_env_context_rewritten` / `codex_env_unknown_child_tag`。非目标（未做）：作息节律塑形（属调度层独立计划）。
- 只改目标应用的镜像引用；配置备份 `/opt/stacks/aether/.env.bak.20260910T153400Z_pre_v0.7.122`（权限 0644）。文件 SHA256：`9b8236924ae5390dbd20f8a301a77fc28aad9c7ec929d2d38460b9379af36701` → `f5a38d3004f0054021f22d64ef274ce84a97ab6b32cb27f7718900f5827deeff`；diff 仅 `APP_IMAGE` 一行，其余逐行一致。既有 `docker-compose.yml` 未改动。
- 仅重建目标应用：`./update.sh --compose-dir /opt/stacks/aether`（默认 compose pull + `up -d` + health wait），全程 exit 0，未出现 `/tmp` 写满导致健康检查探针失败的问题。未更新/重启 PostgreSQL、Redis、sub2api 或其他服务；postgres/redis 容器 ID、启动时间、重启次数在更新前后完全一致（均 restart 0）。
- 本地健康检查 `http://127.0.0.1:20123/health` HTTP 200；启动日志显示 auto database preparation 按既有方式运行，未出现新增迁移或回填日志，未执行数据库降级或新建备份。
- 时区归一化上线自证：启动后即出现一条 `codex_env_tz_resolved timezone="America/New_York" source="tz_env"`，与 §3.1 期望一致；截至核对时未出现 `codex_env_tz_rejected`。
- 更新后约 15 分钟窗口应用日志 112 行，0 ERROR / 0 panic，容器 restart 0；窗口内已承载真实成功请求（`status_code=200` 16 条）。该窗口长度不足以判定 `codex_env_context_rewritten` 的实际改写量级（仅 `codex_env_tz_resolved` 1 条、暂无改写计数命中），按运维手册 §3.7 需在后续更长时间窗（建议 ≥30 分钟且有 Codex Pro/Plus 带 env 块流量）复核 `<timezone>` 单一值与 blocks_removed/inserted 分布。
- 验证边界：本次未经独立代码审查即上线（`codex_environment_context.rs` 系本会话新写，`env-ctx-reviewer` 子代理的只读复核报告在部署后才送达，其 5 条发现均未阻断发布，其中 1（WS 增量步与次轮全量回放同一锚点合成项不一致）、3（`ms + delta` 溢出）为建议优先修项；截至本记录写入时这 5 条尚未在代码中处置）。因此不对该模块的边角路径（WS 跨午夜增量步、异常 `create_time`、HttpCompact 起始插入）作线上正确性背书，待修复与复核后再行更新本条结论。上线一刻在飞线程的历史会被一次性改写（timezone 换、部分 diff 删/插），对上游表现为一次历史编辑，prompt cache 会失一次，之后稳定。
- 回滚参考：上一稳定镜像 `ghcr.io/shirtiny/aether@sha256:252bb5e710c2765c6507c655ee390d3413d0ad48423fb2ec76729a5a5590d8c2`（.120）。如需回滚须另行明确授权，仅恢复 `APP_IMAGE` 并重建 app，无数据库降级。

## 2026-09-11T03:47:08.428593019Z - 更新完成 / backend-v0.7.126

- 授权：用户明确要求「更新线上到最新版本」。发布构建 success 后执行；未移动 tag，未进行灰度或无关升级。
- Actions：[backend-v0.7.126](https://github.com/Shirtiny/Aether/actions/runs/34547149463) `Release Aether` success，revision `9193353504bf70de68d2e973c096cb80dfb94f0f`。线上此前是 `.125`（`7ce7b7e03`），本次更新覆盖 `.123`（本人上一会话改动：turn 时间戳与前缀缓存 id 跟随出站身份、Codex OAuth 账号默认并发 1）、`.124`（用户自己的提交 `739b0a8cd`，"complete codex routing and response header handling"：新增 `x-codex-routing-hint` 头合成与 WS 侧对应实现，`x-openai-internal-codex-residency` 透传，`x-codex-safety-buffering-*` 响应头过滤）、`.125`（用户自己的提交 `7ce7b7e03`，routing hint 归一化走统一 seam 的小修）中此前尚未上线的部分；这三个 tag 均在本会话之外由用户本人直接提交/推送到 `origin/custom`，本次是它们首次上线。
- `.126` 本身内容（用户自己的提交 `919335350`，"honor explicit endpoint header rules after sanitization"）：修复 `apply_codex_pool_runtime_identity`（`apps/aether-gateway/src/ai_serving/planner/standard/codex.rs`）——此前当 Codex 身份合成功能关闭（scope 为 `None`）时，函数提前 `?` 返回，导致 `<environment_context>` 时区/日期归一化与「显式端点请求头规则」这两个后续步骤被一并跳过；改为 scope 为 `None` 时跳过身份改写但仍执行环境上下文归一化（避免 Redis 不可用/未开身份合成时泄露客户端本机时区），并在最后新增一步：以 `authorization`/`api-key`/`x-api-key`/`content-type` 为保护字段，把端点上配置的显式请求头规则在 Codex 身份/头清理之后再应用一遍，防止操作员配置的头被池内清理逻辑悄悄剥掉。
- 更新前：`backend-v0.7.125` / `7ce7b7e037`；镜像 `ghcr.io/shirtiny/aether@sha256:737930e84702d05d5003068d79d10a606635c13de6c8b3070d95b3f5f5268328`；容器已运行约 3 小时，healthy / restart 0。
- 更新后：镜像 `ghcr.io/shirtiny/aether@sha256:5ca2a342d085c74cc265184296dd41bcd9ed0f7a720648faa3da90688b87dea9`（index digest，经 `docker buildx imagetools inspect ghcr.io/shirtiny/aether:backend-v0.7.126` 两次核对一致）；容器 started `2026-09-11T03:47:08.428593019Z`；healthy / restart 0；label `org.opencontainers.image.version=backend-v0.7.126`。
- 只改目标应用的镜像引用；配置备份 `/opt/stacks/aether/.env.bak.20260911T034524Z_pre_v0.7.126`（权限 0644，备份时与更新前 `.env` 内容完全一致，SHA256 相同）；diff 仅 `APP_IMAGE` 一行。既有 `docker-compose.yml` 未改动。
- 首次执行 `update.sh` 时忘记先把 `.env` 的 `APP_IMAGE` 改到 `.126` 摘要，导致该次 pull/recreate 实际重新拉取了原 `.125` 镜像（容器内容未变，等效于一次空转重启，未产生影响）；发现后立即修正 `.env` 后重新执行 `update.sh`，成功拉取并切换到 `.126`。
- 执行前 `/tmp`（tmpfs，1.0G）已到 87%（`sub2api-gocache` 占 823M，`lsof` 确认无进程持有），为避免重演 `.118` 那次因 `/tmp` 写满导致 exec 型健康检查探针失败的故障，先行清理该目录（与本次更新无关的 Go 构建缓存，可再生），清理后降到 7%；本次 `update.sh` 全程未触发健康检查探针失败。未更新/重启 PostgreSQL、Redis、sub2api 或其他服务；postgres/redis 容器 ID、启动时间、重启次数在更新前后完全一致（均 restart 0）。
- 本地健康检查 `http://127.0.0.1:20123/health` HTTP 200；`_sqlx_migrations` 55 成功 / 0 失败，最新 `20260827000000`；`schema_backfills` 5 条；均与更新前一致，无新增迁移或回填，未执行数据库降级或新建备份。
- 更新后 2 分钟窗口应用日志 262 行，0 ERROR / 0 panic，容器 restart 0。
- 本次改动（`.126`）的本地测试：`cargo test -p aether-gateway --lib` 定向套件（`codex_runtime_identity::`、`codex_environment_context::`、`codex_routing_hint::`、`ai_serving::planner::standard::codex::`）106 passed / 0 failed / 1 ignored；未跑全量套件（本机 7GB 内存，`custom` 分支本有既存失败基线，全量套件另行安排）。`.124`/`.125`/`.126` 均系用户本人提交，非本人代码审查产物；`.126` 的 diff 本人已逐行审阅，逻辑与提交信息一致。
- 上线自证：更新前已用一条真实 Codex Pro 请求核对过 `.123` 的两处改动（`turn_started_at_unix_ms` 与出站 `turn_id` 的 UUIDv7 时间戳完全一致；`msg_` 前缀缓存 id 与 `uuid5(uuid5(NAMESPACE_OID, 出站 thread_id), 消息文本)` 完全一致）以及 `.122` 的时区归一化（`<timezone>America/New_York</timezone>`，与容器时区一致）在 `.125` 上均生效；`x-codex-routing-hint` 头（`.124`）在该请求中也已出现，值与请求体 `model`/`service_tier` 一致。`.126` 本身触及的「身份合成关闭时环境上下文归一化被跳过」与「显式端点头规则被覆盖」两条边界，尚未见真实流量命中（依赖特定配置组合），留待后续观察。
- 回滚参考：上一稳定镜像 `ghcr.io/shirtiny/aether@sha256:737930e84702d05d5003068d79d10a606635c13de6c8b3070d95b3f5f5268328`（.125）。如需回滚须另行明确授权，仅恢复 `APP_IMAGE` 并重建 app，无数据库降级。

## 2026-09-12T01:34:41.306401403Z - 更新完成 / backend-v0.7.130

- 授权：用户明确指令「开始更新线上容器」（在 `.130` 的 Actions 核对为 success 之后）。未移动 tag，未做灰度或无关升级。
- Actions：[backend-v0.7.130](https://github.com/Shirtiny/Aether/actions/runs/34633652934) `Release Aether` success（约 32 分钟），revision `91a05842c077ee09e4cc98b3d49254b7ae7e16f6`。线上此前是 `.126`（`919335350`）；本次一并首次上线 `.127`（用户提交 `c3bd997d8`：按号池账号合成 `workspaces`、客户端发版跟随、WS 握手 UA，以及**按模型名单加 `x-openai-internal-codex-responses-lite` 头**）、`.128`（`ef552413b`，并行会话：WS ClientHello 密钥共享顺序对齐官方 CLI）、`.129`（`8294ed0bb`，并行会话：WS 路由耗尽先行标记）。
- `.130` 本身内容（本会话提交 `91a05842c`「match official cli wire shape on chatgpt hosts」，依据官方直登 0.154.0 抓包分析仓 + codex-rs 源码）：WS 握手头按官方顺序且不带 installation-id / responses-lite / cookie；HTTP 头按官方 codex-cli 顺序（控制头 `x-aether-execution-header-order`）；`/responses` body zstd level 3；按账号隔离的 Cloudflare cookie jar（`execution_runtime/chatgpt_cloudflare_cookies.rs`）；`x-codex-installation-id` 请求头只留 `/responses/compact`。kill switch `AETHER_CODEX_TRANSPORT_FIDELITY=off`。
- 更新前：`backend-v0.7.126` / `9193353504bf`；镜像 `ghcr.io/shirtiny/aether@sha256:5ca2a342d085c74cc265184296dd41bcd9ed0f7a720648faa3da90688b87dea9`；容器 started `2026-09-11T03:47:08.428593019Z`，运行约 21h47m，healthy / restart 0。
- 更新后：镜像 `ghcr.io/shirtiny/aether@sha256:58d3918a14732ca0613908258226a2a56849185e2712cd1c075d37781ca35e81`（index digest）；容器 `a88329e9a9fd…` started `2026-09-12T01:34:41.306401403Z`；healthy / restart 0；label `org.opencontainers.image.version=backend-v0.7.130`、`revision=91a05842c077…`。`update.sh --compose-dir /opt/stacks/aether` 01:34:27Z 开始、01:34:47Z 退出码 0，`--wait` 一次通过，未触发回退分支。
- 只改目标应用的镜像引用；配置备份 `/opt/stacks/aether/.env.bak.20260912T013354Z_pre_v0.7.130`（0644，2527 字节，与更新前 `.env` SHA256 相同）；diff 仅 `APP_IMAGE` 一行。`docker-compose.yml` 未改。未更新/重启 PostgreSQL、Redis、sub2api；postgres（`18afea5bc9b0…`，started 2026-09-08T14:59:10Z）/ redis（`94725b145b2c…`，started 2026-06-27）容器 ID、启动时间、restart 0 前后一致。
- 本地健康检查 `http://127.0.0.1:20123/health` HTTP 200；`_sqlx_migrations` 55 成功 / 0 失败，最新 `20260827000000`；`schema_backfills` 5 条；与更新前一致（`.126`→`.130` 无 SQL 变更），未做数据库备份或降级。
- 更新后日志窗口：0 ERROR / 0 panic；启动即见 `codex_env_tz_resolved tz=America/New_York source=tz_env`；5 条 WARN 为既有的第三方 provider key 问题，与本次无关。
- 本地测试（`.130`）：`cargo test -p aether-gateway --lib` 定向套件 157 passed / 0 failed（`chatgpt_cloudflare_cookies`、`execution_runtime::transport::tests`、`planner::standard::codex::tests`、`codex_ws::runtime::tests`、`codex_profile::tests`）；未跑全量。
- **上线后发现回归（源自 `.127`，随本次首次上线）**：01:38:19Z–01:39:07Z 三条 `gpt-5.6-sol` 请求全部 400，上游报「X-OpenAI-Internal-Codex-Responses-Lite requires `reasoning.context` to be `all_turns`」。三条均来自同一用户的 `Go-http-client/1.1`（非官方客户端）；同一时段官方 `Codex Desktop/0.153.4` 的 16 条 sol 请求全部 200。根因：`codex_routing_hint.rs` 按静态模型名单给 sol/luna/terra/astra/daybreak/codex-auto-review 加该头，而官方 codex-rs 加该头时同时把 body 改成 lite 契约（`reasoning.context = all_turns` 等），网关不改 body，第三方客户端的 body 缺 `all_turns` 即被拒。更新前 24h 这几个目标模型约 1.17 万条请求来自非官方客户端（sol 6524、luna 2584、astra 1968、codex-auto-review 603），凡 body 不带 `all_turns` 者在 `.130` 下都会 400。更新后 16 分钟内 Codex 整体 78×200 / 3×400（流量低谷）。与 `.130` 的传输层改动无关。处置：`.131` 热修改为「该头只跟随 body 的 `reasoning.context == all_turns`」（runbook §3.12），提交 / tag 后等待授权上线；未回滚。
- 其他观察：Codex Plus 更新后最初约 10 分钟 44×200 / 3×400，cache_read 占比 64.3%（更新前 30 分钟 82.7%，样本小，待更长窗口复核）；Codex Pro 自 2026-09-11 02:43 EDT 起无流量，`.130` 的 zstd / cookie / 头顺序在 Pro 上尚无线上验证。
- 回滚参考：上一稳定镜像 `ghcr.io/shirtiny/aether@sha256:5ca2a342d085c74cc265184296dd41bcd9ed0f7a720648faa3da90688b87dea9`（.126）。如需回滚须另行明确授权，仅恢复 `APP_IMAGE` 并重建 app，无数据库降级；回滚会同时撤掉 `.127`–`.130` 的全部功能。

## 2026-09-12T02:53:11.452986572Z - 更新完成 / backend-v0.7.131

- 授权：用户明确指令「actions成功了 更新线上到最新版本」（在 `.131` 的 Actions 核对为 success 之后）。未移动 tag，未做灰度或无关升级。
- Actions：[backend-v0.7.131](https://github.com/Shirtiny/Aether/actions/runs/34666914883) `Release Aether` success（02:10Z 起约 36 分钟，02:45:43Z 完成），revision `c72543b787a66ba38cde98d548fd07bfc6839ef4`。相对 `.130` 只改 4 个文件（`codex_routing_hint.rs`、`planner/.../responses/decision/request.rs`、runbook §3.12、本文件），无 SQL 迁移。
- `.131` 内容：热修 `.127` 回归——`x-openai-internal-codex-responses-lite` 不再按静态模型名单添加，只在请求体 `reasoning.context == "all_turns"` 时随体发送（HTTP `apply_to_decision` 与 WS 候选头同一规则），删除 `RESPONSES_LITE_MODELS` 名单。传输层（`.130`）未动。
- 更新前：`backend-v0.7.130` / `91a05842c077`；镜像 `ghcr.io/shirtiny/aether@sha256:58d3918a14732ca0613908258226a2a56849185e2712cd1c075d37781ca35e81`；容器 `a88329e9a9fd…` started `2026-09-12T01:34:41.306401403Z`，运行约 1h18m，healthy / restart 0。
- 更新后：镜像 `ghcr.io/shirtiny/aether@sha256:b38bbfd7056e851294c4d46d9cd6f869b725145200d4f706803f0251906af65b`（index digest；linux/amd64 manifest `394e688770f5…`）；容器 `84d3697ec735…` started `2026-09-12T02:53:11.452986572Z`；healthy / restart 0；label `org.opencontainers.image.version=backend-v0.7.131`、`revision=c72543b787a6…`、`created=2026-09-12T02:44:55Z`。`update.sh --compose-dir /opt/stacks/aether` 02:52:57Z 开始、02:53:17Z 退出码 0，`--wait` 一次通过，未触发回退分支。
- 只改目标应用的镜像引用；配置备份 `/opt/stacks/aether/.env.bak.20260912T025139Z_pre_v0.7.131`（0644，2527 字节，与更新前 `.env` SHA256 相同）；diff 仅 `APP_IMAGE` 一行。`docker-compose.yml` 未改。未更新/重启 PostgreSQL、Redis、sub2api；postgres（`18afea5bc9b0…`，started 2026-09-08T14:59:10Z）/ redis（`94725b145b2c…`，started 2026-06-27）容器 ID、启动时间、restart 0 前后一致。
- 本地健康检查 `http://127.0.0.1:20123/health` HTTP 200；`_sqlx_migrations` 55 成功 / 0 失败，最新 `20260827000000`；`schema_backfills` 5 条；与更新前一致，未做数据库备份或降级。
- 启动日志：0 ERROR / 0 panic；`codex_env_tz_resolved timezone=America/New_York source=tz_env`；9 条 WARN 为 `usage::reporting`（4）与 `model_fetch::runtime`（5，第三方 provider 模型列表拉取）既有类型，与本次无关。
- 更新前 60 分钟基线（`.130`）：Codex 599×200 / 13×400。其中 1 条为 lite 400 签名，来自**官方旧版** `codex-tui/0.146.0 (Mac OS)` 打 `gpt-5.6-sol`（planner 加了 lite 头、body 非 lite）——说明 `.127` 的名单法不止打断第三方客户端，也打断未启用 lite 的旧版官方客户端；其余 12 条为 WS 路径 luna 400（`.130` 条目里的待查项，与 lite 无关）。sol 在 `.130` 下 51/53 条 planner 加头。
- 更新后首分钟：Codex 9×200 / 0×400；sol 4 条：3 条 Codex Desktop 0.153.4 带 lite 头 200，1 条 `Go-http-client/2.0` **不带** lite 头 200——正是 `.130` 下会 400 的请求形状，热修在线上生效。
- **上线后发现新回归（源自 `.131` 本身）**：02:55:32Z 起官方 `Codex Desktop/0.153.4 (Mac OS)` 发到 `gpt-5.6-luna` 的请求 400「This model is not supported when using X-OpenAI-Internal-Codex-Responses-Lite.」，更新后 7 分钟内 3 条（Codex 整体 28×200 / 3×400）；过去 7 天从未出现该签名。根因：号池把 luna 映射到 `gpt-5.5`（`usage.target_model`；24h 内 luna→gpt-5.5 8872 条 200 / 283 err，terra→gpt-5.5 3524 / 305，luna→luna 2207 / 381），客户端按 luna 的 manifest 造了 lite body，`.131` 只看 body 就加头，而目标 `gpt-5.5` 不支持 lite——后端拒绝头、接受不带头的 lite body（`.130` 下 luna→gpt-5.5 无头 960×200；`.131` 下 luna→luna 带头 2×200，luna→gpt-5.5 带头 3×400、无头 15×200）。潜在影响：所有 lite 形状的 luna/terra 请求凡被映射到 `gpt-5.5` 都会 400，即官方 0.153.x 客户端的主流量。处置：`.132` 改为「lite body **且** 出站目标模型在 lite 名单」才加头（runbook §3.12），提交 / tag 后等待授权上线；未回滚（回滚到 `.130` 会重新引入 sol 的「有头没体」400）。
- 回滚参考：上一镜像 `ghcr.io/shirtiny/aether@sha256:58d3918a14732ca0613908258226a2a56849185e2712cd1c075d37781ca35e81`（.130）。如需回滚须另行明确授权，仅恢复 `APP_IMAGE` 并重建 app，无数据库降级；回滚到 .130 会重新引入 lite 400。
