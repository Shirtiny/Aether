[package]
name = "tungstenite"
description = "Lightweight stream-based WebSocket implementation"
categories = ["web-programming::websocket", "network-programming"]
keywords = ["websocket", "io", "web"]
authors = ["Alexey Galakhov", "Daniel Abramov"]
license = "MIT OR Apache-2.0"
readme = "README.md"
homepage = "https://github.com/snapview/tungstenite-rs"
documentation = "https://docs.rs/tungstenite/0.27.0"
repository = "https://github.com/snapview/tungstenite-rs"
version = "0.27.0"
edition = "2021"
rust-version = "1.75"
include = ["benches/**/*", "src/**/*", "examples/**/*", "LICENSE-*", "README.md", "CHANGELOG.md"]

[package.metadata.docs.rs]
all-features = true

[package.metadata.aether-vendor]
base_repository = "https://github.com/openai-oss-forks/tungstenite-rs"
base_revision = "4fffad30fe373adbdcffab9545e9e9bf4f2fc19f"
patch_id = "aether-tungstenite-0.27-out-buffer-retention-v1"

[features]
default = ["handshake"]
handshake = ["data-encoding", "headers", "httparse", "sha1"]
headers = ["http", "dep:headers"]
proxy = ["handshake"]
url = ["dep:url"]
native-tls = ["native-tls-crate"]
native-tls-vendored = ["native-tls", "native-tls-crate/vendored"]
rustls-tls-native-roots = ["__rustls-tls", "rustls-native-certs"]
rustls-tls-webpki-roots = ["__rustls-tls", "webpki-roots"]
__rustls-tls = ["rustls", "rustls-pki-types"]
deflate = ["headers", "flate2"]

[dependencies]
data-encoding = { version = "2", optional = true }
bytes = "1.9.0"
headers = { version = "0.4.0", optional = true }
http = { version = "1.0", optional = true }
httparse = { version = "1.3.4", optional = true }
log = "0.4.8"
rand = "0.9.0"
sha1 = { version = "0.10", optional = true }
thiserror = "2.0.7"
url = { version = "2.1.0", optional = true }
utf-8 = "0.7.5"

[dependencies.flate2]
optional = true
version = "1.1.0"
default-features = false
# permessage-deflate needs a backend that supports setting the context window
# size without carrying flate2's native libz-sys backend into
# wasm32-unknown-unknown, where zlib has no libc sysroot.
features = ["zlib-rs"]

[dependencies.native-tls-crate]
optional = true
package = "native-tls"
version = "0.2.3"

[dependencies.rustls]
optional = true
default-features = false
features = ["std"]
version = "0.23.0"

[dependencies.rustls-pki-types]
optional = true
version = "1.0"

[dependencies.rustls-native-certs]
optional = true
version = "0.8.0"

[dependencies.webpki-roots]
optional = true
version = "0.26"

[dev-dependencies]
http = "1.0"
httparse = "1.3.4"
rand = "0.9.0"
